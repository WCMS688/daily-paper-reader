---
title: "CapBench: A Multi-PDK Dataset for Machine-Learning-Based Post-Layout Capacitance Extraction"
title_zh: CapBench：一个用于基于机器学习的版图后电容提取的多 PDK 数据集
authors: "Hector R. Rodriguez, Jiechen Huang, Wenjian Yu"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11202v1"
tags: ["query:si-pi"]
score: 8.5
evidence: 基于机器学习的芯片设计后仿真电容提取
tldr: 本研究推出了CapBench，一个用于机器学习后布局电容提取的多PDK数据集。该数据集涵盖ASAP7、NanGate45和Sky130HD三个工艺节点，包含从开源设计中提取的6万多个3D窗口。研究团队利用高精度随机游走求解器生成标签，并提供密度图、图和点云等多种表示形式。通过评估10种ML架构，展示了不同模型在精度与速度之间的权衡，为该领域的迁移学习和可扩展性研究提供了重要基准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11202v1/fig-001.webp\", \"caption\": \"Table 6: Performance comparison of ML architectures on small windows. MAPE is computed for total capacitance.\", \"page\": 6, \"index\": 1, \"width\": 504, \"height\": 409}, {\"url\": \"assets/figures/arxiv/2604.11202v1/fig-002.webp\", \"caption\": \"Figure 2: CapBench dataset generation flow and toolchain.\", \"page\": 3, \"index\": 2, \"width\": 446, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2604.11202v1/fig-003.webp\", \"caption\": \"Table 3: RWCap runtime per window.\", \"page\": 4, \"index\": 3, \"width\": 452, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.11202v1/fig-004.webp\", \"caption\": \"Table 2: Window size and count.\", \"page\": 4, \"index\": 4, \"width\": 500, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2604.11202v1/fig-005.webp\", \"caption\": \"Figure 3: Window placement on a 2×2 grid.\", \"page\": 4, \"index\": 5, \"width\": 404, \"height\": 306}]"
motivation: 旨在解决机器学习辅助电容提取领域缺乏高质量、多工艺节点且完全可复现的基准数据集的问题。
method: 基于OpenROAD流程生成14个布局，提取多尺寸3D窗口，并使用经工业标准验证的RWCap随机游走求解器生成高精度电容标签。
result: "实验表明CNN在电容预测中精度最高（误差1.75%），而GNN虽然推理速度提升达41.4倍，但误差也增加至10.2%。"
conclusion: CapBench为开发高效准确的电容提取模型提供了多模态数据支持，并揭示了当前主流ML架构在处理此类物理设计问题时的性能权衡。
---

## 摘要
我们提出了 CapBench，这是一个用于电容提取的完全可复现、多 PDK 数据集。该数据集源自开源设计，包括单核 CPU、片上系统（SoC）和媒体加速器。所有设计均使用 14 个独立的 OpenROAD 流程运行完成布局布线，涵盖三个工艺节点：ASAP7、NanGate45 和 Sky130HD。我们从这些版图中提取了分布在三个尺寸层级的 61,855 个 3D 窗口，以支持迁移学习和可扩展性研究。高保真电容标签使用最先进的随机游走求解器 RWCap 生成，并经过工业标准工具 Raphael 的验证，总电容的平均绝对误差为 0.64%。每个窗口都被预处理为密度图、图表示和点云。我们评估了 10 种机器学习架构以展示数据集的用法并作为基准，包括卷积神经网络（CNN）、点云 Transformer 和图神经网络（GNN）。CNN 表现出最低的误差（1.75%），而 GNN 的速度快达 41.4 倍，但误差较大（10.2%），这说明了明显的精度-速度权衡。代码和数据集可在 https://github.com/THU-numbda/CapBench 获取。

## Abstract
We present CapBench, a fully reproducible, multi-PDK dataset for capacitance extraction. The dataset is derived from open-source designs, including single-core CPUs, systems-on-chip, and media accelerators. All designs are fully placed and routed using 14 independent OpenROAD flow runs spanning three technology nodes: ASAP7, NanGate45, and Sky130HD. From these layouts, we extract 61,855 3D windows across three size tiers to enable transfer learning and scalability studies. High-fidelity capacitance labels are generated using RWCap, a state-of-the-art random-walk solver, and validated against the industry-standard Raphael, achieving a mean absolute error of 0.64% for total capacitance. Each window is pre-processed into density maps, graph representations, and point clouds. We evaluate 10 machine learning architectures that illustrate dataset usage and serve as baselines, including convolutional neural networks (CNNs), point cloud transformers, and graph neural networks (GNNs). CNNs demonstrate the lowest errors (1.75%), while GNNs are up to 41.4x faster but exhibit larger errors (10.2%), illustrating a clear accuracy-speed trade-off. Code and dataset are available at https://github.com/THU-numbda/CapBench.

---

## 论文详细总结（自动生成）

这是一份关于论文《CapBench: A Multi-PDK Dataset for Machine-Learning-Based Post-Layout Capacitance Extraction》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在电子设计自动化（EDA）领域，基于机器学习（ML）的寄生电容提取虽具潜力，但面临**高质量、多工艺节点（PDK）、完全开源且可复现的数据集匮乏**的问题。
*   **研究背景**：传统的 3D 场求解器精度高但速度极慢，而现有的开源数据集（如 CircuitNet）要么缺乏完整的版图几何信息，要么缺乏高精度的电容标签，且大多依赖私有 PDK，限制了学术界的复现与对比。
*   **整体含义**：CapBench 旨在通过提供一个涵盖多种工艺、多种表示形式（密度图、图、点云）及高精度标签的大规模数据集，为 ML-for-EDA 研究建立一个标准化的基准平台。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：利用全开源工具链（OpenROAD）和开源 PDK 生成真实版图，通过滑动窗口技术提取局部 3D 几何结构，并结合高精度随机游走求解器生成金标准标签。
*   **关键流程**：
    1.  **设计选择**：选取 Ibex、CVA6、RocketChip 等开源 CPU 和 SoC 设计。
    2.  **版图生成**：使用 OpenROAD 流程在 ASAP7 (7nm)、NanGate45 (45nm) 和 Sky130HD (130nm) 三个节点上完成综合、布局和布线。
    3.  **窗口提取**：使用 KLayout 提取不同尺寸（Small/Medium/Large）的 3D 窗口，确保不重叠以防止数据泄露。
    4.  **标签生成**：采用 **RWCap**（基于浮动随机游走算法的场求解器）生成电容标签。
    5.  **多模态预处理**：将窗口转化为三种格式：**2D 密度图**（用于 CNN）、**空间图结构**（用于 GNN）和**点云**（用于 PCT）。
*   **技术细节**：RWCap 标签经过工业级工具 Raphael 的验证，确保了总电容误差低于 1%。

### 3. 实验设计与 Benchmark
*   **数据集规模**：共提取 61,855 个 3D 窗口。
*   **对比架构**：评估了 10 种代表性 ML 模型：
    *   **CNN 家族**：ResNet-34, ResNet-50, ResNet-101。
    *   **点云 Transformer (PCT)**：具有 4、6、8、10 层注意力的变体。
    *   **GNN 家族**：GCN, GAT, GATv2（引入了自注意力池化机制）。
*   **基准对比**：以 Raphael 求解器的结果为绝对参考，对比了 RWCap 的精度以及 OpenROAD 内置提取器 OpenRCX 的误差。

### 4. 资源与算力
*   **硬件环境**：
    *   **标签生成**：服务器配备两颗 AMD EPYC 7V12 (64核) 处理器，512 GB RAM。
    *   **模型训练与推理**：服务器配备 **4 张 NVIDIA RTX 4090 GPU**。
*   **训练细节**：所有模型从头训练 50 个 epoch，使用 Adam 优化器，学习率为 1e-4。
*   **时长/吞吐量**：文中详细记录了各模型的推理吞吐量（samples/s），例如 ResNet-34 在 Sky130HD 上可达 4602 samples/s。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种 PDK、3 种窗口尺寸、10 种模型架构。
*   **充分性评价**：
    *   **客观性**：通过 80/20 的窗口级划分避免了空间数据泄露（Data Leakage），这是以往研究中常被忽视的。
    *   **公平性**：在相同的硬件和超参数框架下对比了不同模态的模型，并对 RWCap 标签进行了严谨的工业级验证。
    *   **局限性**：虽然涵盖了多 PDK，但主要实验集中在 Small 窗口上，对跨工艺（Cross-PDK）迁移学习的实验展示相对较少。

### 6. 主要结论与发现
*   **精度表现**：**CNN (ResNet-50)** 表现最佳，在 Sky130HD 上的平均绝对百分比误差 (MAPE) 仅为 **1.75%**。
*   **速度表现**：**GNN** 具有显著的速度优势，其推理速度比 CNN 快约 **41.4 倍**（因为 GNN 能一次性预测窗口内所有导体的电容），但误差较高（约 10.2%）。
*   **权衡关系**：存在明显的“精度-速度”权衡。PCT 模型虽然参数量小，但由于自注意力机制的计算开销，推理速度反而慢于 CNN。
*   **工具对比**：OpenROAD 默认的 OpenRCX 误差接近 20%，证明了使用高精度场求解器生成标签的必要性。

### 7. 优点（亮点）
*   **全开源与可复现**：从 RTL 到 PDK 再到工具链完全开源，解决了 EDA 领域长期存在的私有数据壁垒。
*   **多模态支持**：同时提供图像、图和点云数据，极大方便了不同背景（CV、Graph、3D）的研究者介入。
*   **高质量标签**：使用 RWCap 并经过 Raphael 验证，确保了基准数据的权威性。
*   **多尺度设计**：提供三种尺寸的窗口，支持研究模型在不同感知野下的表现。

### 8. 不足与局限
*   **几何分辨率限制**：PCT 模型在 1024 个采样点下表现不佳，说明对于复杂的 3D 电容问题，点云采样密度可能是一个瓶颈。
*   **GNN 训练信号较弱**：实验中 GNN 采用全网（Net-level）标签而非节点级标签，导致其训练难度加大，精度尚未达到最优。
*   **工艺覆盖**：虽然包含 ASAP7，但大部分 ML 性能分析集中在 NanGate45 和 Sky130HD，对先进制程（7nm）下的特殊物理效应（如多重曝光）探讨略显不足。

（完）
