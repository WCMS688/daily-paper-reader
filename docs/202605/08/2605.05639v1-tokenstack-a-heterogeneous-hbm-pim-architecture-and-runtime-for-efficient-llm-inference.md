---
title: "TokenStack: A Heterogeneous HBM-PIM Architecture and Runtime for Efficient LLM Inference"
title_zh: TokenStack：一种用于高效大语言模型推理的异构 HBM-PIM 架构与运行时
authors: "Zhuoran Li, Zhuohang Bian, Zihao Huang, Guangyu Sun, Yun Liang, Youwei Zhuo"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05639v1"
tags: ["query:si-pi"]
score: 6.0
evidence: 用于高带宽内存任务的HBM-PIM架构
tldr: 本研究针对大语言模型推理中KV缓存带来的带宽和容量瓶颈，提出了TokenStack架构。它是一种垂直异构的HBM-PIM设计，利用HBM4逻辑层将存储堆栈划分为高密度存储层和PIM计算层。通过逻辑层控制器实现跨层数据管理、内联量化及拓扑感知的KV放置策略，有效平衡了计算效率与存储容量，在提升推理吞吐量的同时显著降低了能耗。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.05639v1/fig-001.webp\", \"caption\": \"Figure 11: Normalized p50 TTFT (top) and TBT (bottom) vs. QPS for Devstral-123B and Qwen3-32B. TTFT dominates the gap.\", \"page\": 11, \"index\": 1, \"width\": 1046, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2605.05639v1/fig-002.webp\", \"caption\": \"Figure 9: Normalized p50 end-to-end latency vs. QPS (normalized to TokenStack’s minimum). TokenStack stays flat; AttAcc\", \"page\": 10, \"index\": 2, \"width\": 1046, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2605.05639v1/fig-003.webp\", \"caption\": \"Figure 7: Prompt and generation length distributions across\", \"page\": 8, \"index\": 3, \"width\": 500, \"height\": 255}, {\"url\": \"assets/figures/arxiv/2605.05639v1/fig-004.webp\", \"caption\": \"Figure 3: TokenStack system architecture. Each stack combines dense capacity layers, PIM-enabled compute layers, and a\", \"page\": 4, \"index\": 4, \"width\": 921, \"height\": 494}]"
motivation: 现有的HBM-PIM架构在处理LLM推理时，存在PIM逻辑资源浪费或GPU可用带宽受限的问题，难以高效应对KV缓存的扩展需求。
method: 提出一种垂直异构架构，将HBM堆栈分为存储层和PIM层，并利用逻辑基座芯片进行跨层数据管理、地址转换及拓扑感知的KV缓存调度。
result: "实验表明，TokenStack相比AttAcc将平均Token吞吐量提高了1.62倍，SLO达标容量提升了1.70倍，且单Token能耗降低了30-47%。"
conclusion: TokenStack通过异构硬件设计与智能运行时的结合，为解决LLM推理中的KV缓存瓶颈提供了一种高带宽、低功耗且高容量的存储计算方案。
---

## 摘要
大语言模型（LLM）服务目前受限于键值（KV）缓存。在解码过程中，每个新生成的 token 都会重新读取之前的 KV 状态，因此注意力机制（attention）变成了一项对带宽和容量要求极高的内存任务。HBM-PIM 通过将注意力计算移至靠近内存的位置来提供帮助，但目前的堆栈组织方式仍然存在资源浪费。实际上，只有热点 KV 块能从近存计算中获益，而权重、激活值和冷 KV 块主要需要高密度存储和 GPU 可见的带宽。统一的 HBM-PIM 堆栈使得所有层都要为 PIM 逻辑付出代价，而专用 PIM 设计（如 AttAcc）虽然恢复了容量，但缩小了留给 GPU 端工作的 HBM 带宽。我们提出了 TokenStack，这是一种针对以 KV 为中心的 LLM 服务而设计的垂直异构 HBM-PIM 架构，它利用了 HBM4 的逻辑基片。TokenStack 将每个堆栈分为高密度容量层和支持 PIM 的计算层，并使用逻辑底座芯片作为堆栈本地控制点，在没有主机端开销的情况下管理跨层数据移动。底座芯片控制器处理跨层 DMA、分层地址转换、注意力端的聚合/广播协调以及迁移过程中的在线量化。在硬件之上，TokenStack 使用拓扑感知的 KV 放置、负载感知的驱逐和有界复制，将热点 KV 保持在 PIM 计算附近，同时将较冷的状态移动到高密度层。使用来自生产环境的四个模型轨迹，多 QPS 运行结果显示，TokenStack 相比 AttAcc 将几何平均 token 吞吐量提高了 1.62 倍，满足服务等级目标（SLO）的服务容量提高了 1.70 倍，并将每个 token 的能耗降低了 30-47%。

## Abstract
Large language model (LLM) serving is now limited by the key-value (KV) cache. During decode, each new token rereads prior KV state, so attention becomes a bandwidth- and capacity-heavy memory task. HBM-PIM helps by moving attention closer to memory, but current stack organizations still waste resources. In practice, only hot KV blocks benefit from near-memory compute. Weights, activations, and cold KV mainly need dense storage and GPU-visible bandwidth. A uniform HBM-PIM stack makes all layers pay for PIM logic, while a dedicated-PIM design such as AttAcc recovers capacity but shrinks the HBM bandwidth left for GPU-side work. We propose TokenStack, a vertically heterogeneous HBM-PIM architecture for KV-centric LLM serving that leverages HBM4's logic-die substrate. TokenStack separates each stack into dense capacity layers and PIM-enabled compute layers, then uses the logic base die as a stack-local control point that manages cross-layer movement without host-side overhead. The base-die controller handles cross-layer DMA, layered address translation, attention-side gather/broadcast coordination, and inline quantization during migration. On top of this hardware, TokenStack uses topology-aware KV placement, workload-aware eviction, and bounded replication to keep hot KV near PIM compute while moving colder state to dense layers. Using production-derived traces across four models, completed multi-QPS runs show that TokenStack increases geometric-mean token throughput by 1.62x and SLO-compliant serving capacity by 1.70x over AttAcc, and reduces per-token energy by 30-47%.