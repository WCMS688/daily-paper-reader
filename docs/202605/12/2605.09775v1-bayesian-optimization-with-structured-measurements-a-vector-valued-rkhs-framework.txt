Title: Bayesian Optimization with Structured Measurements: A Vector-Valued RKHS Framework

URL Source: https://arxiv.org/pdf/2605.09775v1

Published Time: Tue, 12 May 2026 01:53:03 GMT

Number of Pages: 30

Markdown Content:
# Bayesian Optimization with Structured Measurements: A Vector-Valued RKHS Framework 

Wenbin Wang ∗

Automatic Control Laboratory, EPFL 

Colin N. Jones 

Automatic Control Laboratory, EPFL 

## Abstract 

Bayesian optimization (BO) is an efficient framework for optimizing expensive black-box functions. However, it is typically formulated as learning an end-to-end mapping from inputs to scalar objectives, thereby discarding the potentially rich information whenever a structured system output is available. In this work, we study Bayesian optimization over a vector-valued operator with structured measurements, where each measurement observes multidimensional or functional outputs, e.g., trajectories or spatial fields, rather than a single scalar value. The objective is then defined as a linear functional of these measurements. This allows each observation to reveal substantially richer information about the underlying system compared to scalar observations. Assuming the unknown operator lies in a vector-valued reproducing kernel Hilbert space (RKHS), we derive high-probability concentration bounds for the kernel ridge regression (KRR) estimator directly in the measurement space, characterizing uncertainty in a general Hilbert space. Building on these results, we propose an algorithm based on the upper confidence bound (UCB) acquisition function with regret guarantees under mild assumptions, recovering sublinear rates for common kernels. Empirically, we demonstrate that leveraging structured measurements leads to improved sample efficiency by enabling efficient transfer of information across objectives and adaptation to time-varying settings. 

## 1 Introduction 

Bayesian optimization (BO) is an efficient sequential strategy for optimizing expensive black-box functions [ 40 , 10 , 15 , 16 ]. It is commonly formulated as learning an end-to-end mapping from inputs to a scalar-valued objective using a Gaussian process surrogate model [ 10 , 40 ]. However, in many real-world problems, this scalar objective is not directly generated by the system, but instead arises as a functional of an underlying structured output, such as a trajectory, field, or time series. For example, in building operation, the cost is defined by integrating heating prices over realized heating power trajectories [ 39 , 44 ]. Standard BO treats the problem as learning a direct mapping from inputs to scalar objective, effectively discarding the intermediate trajectory [ 48 , 46 , 32 , 19 , 5]. This limitation becomes particularly pronounced when multiple objectives correspond to different functionals of the same trajectory (e.g., different price signals), where existing methods must relearn each objective separately instead of sharing information through the common structure. While algorithms such as multi-task Bayesian optimization [ 11 , 14 , 12 , 41 ] partially address this issue by modeling correlations across objectives, they typically rely on a finite number of scalar observations and a careful design of the correlation matrix. Another line of work models such correlations in the input space, such as contextual Bayesian optimization [ 28 , 45 , 44 ], which can be sample-inefficient for the exploration across different contexts. When structured outputs are available for measurement, each observation can reveal more information about the underlying system [ 49 ]. This motivates leveraging such measurements in algorithm design, which can potentially improve learning efficiency.  

> ∗Correspondence to wenbin.wang@epfl.ch .Preprint.
> arXiv:2605.09775v1 [cs.LG] 10 May 2026

To account for structured measurements, it is natural to consider Bayesian optimization with vector-valued outputs [ 24 , 8, 33 ], where the black-box operator maps each input to outputs in a Hilbert space. This formulation enables modeling the underlying system through vector-valued observations. However, existing approaches remain limited. Most work focuses on finite-dimensional Euclidean output spaces [ 29 , 11 , 41 , 38 ], whereas many practical problems involve vector-valued trajectories and functions. Another line of work considers infinite-dimensional outputs but assumes that measurements are available only through linear functionals [ 4, 20 , 30 ], effectively reducing each measurement to a scalar value. As a result, these methods fail to exploit the full structure of the underlying output and limit the amount of information obtained from each observation. In this work, we study Bayesian optimization for black-box systems that produce structured, vector-valued outputs. We introduce a general framework in which the unknown operator lies in a vector-valued reproducing kernel Hilbert space (RKHS) and is observed through a known linear measurement operator, possibly mapping into a different Hilbert space. The objective is then defined as a linear functional of these measurements. This formulation unifies a broad class of existing problem settings [4, 20 , 11 , 42 , 30 , 29 , 41 , 38 ] and enables structured measurements to be incorporated in a principled manner. Under mild assumptions, we establish high-probability concentration bounds for the vector-valued kernel ridge regression (KRR) estimator under general measurement operators. From the derived uncertainty bound, we propose an algorithm based on the upper confidence bound (UCB) of the estimator and prove sublinear regret guarantees for commonly used kernels. Numerical experiments demonstrate that structured measurements significantly improve sample efficiency, permitting information transfer across objectives and adaptation in time-varying settings. 

Contributions. The contributions of this work are as follows: 1. Bayesian optimization with structured measurements. We introduce a general for-mulation of Bayesian optimization in which measurements are obtained through linear measurement operators acting on a latent nonlinear vector-valued operator. This enables richer information to be incorporated at each observation and naturally provides the potential for improving learning efficiency. 2. Theoretical guarantees under general measurement operators. We establish high-probability concentration bounds for vector-valued KRR under arbitrary bounded linear measurement operators in a general Hilbert space. Based on these results, we propose a UCB-type algorithm and prove sublinear regret guarantees, extending classical scalar-valued BO with structured observations. 3. Improved efficiency in real-world applications. Through synthetic benchmarks and real-world experiments, we demonstrate that the proposed method significantly improves sample efficiency by facilitating efficient information transfer across different objectives and achieving superior performance in optimizing time-varying objectives. 

1.1 Related work Learning nonlinear vector-valued operators. Learning vector-valued or function-valued operators has been widely studied in the functional data analysis (FDA) literature [ 18 , 36 ]. Classical approaches typically rely on discretization, representing the outputs in finite-dimensional Euclidean spaces [ 23 , 22 , 31 ]. Some recent work models functions directly in Hilbert spaces [ 4, 24 ], using tools such as multi-output Gaussian processes [ 3, 37 ] and neural operators [ 27 ]. While these methods enable flexible modeling of vector-valued operators, they primarily focus on supervised learning and do not address sequential optimization problems. BO with vector-valued outputs. Several works extend Bayesian optimization to structured or multi-output settings. Multi-task Bayesian optimization [ 11 , 38 , 41 ] leverages correlations across multiple outputs to improve sample efficiency, while multi-objective Bayesian optimization [ 25 , 13 ] focuses on learning Pareto-optimal trade-offs. However, these approaches typically assume a finite number of scalar outputs and do not consider measurements such as functions or trajectories. BO with augmented inputs. A related line of work assumes that observations are obtained through linear functionals of the vector-valued output [ 20 , 30 ]. They view the linear functional as the augmented input of a scalar-valued function with an augmented input space, which reduces the problem to a scalar-valued setting. Consequently, this formulation cannot quantify the uncertainty of the vector-valued operator. A comprehensive analysis of uncertainty quantification under general linear measurement operators is still missing. 22 Problem formulation 

Problem formulation. We consider the problem of optimizing a black-box system in which, for each input, the objective is defined as a linear functional of structured measurements of an underlying vector-valued operator. Specifically, we consider 

max  

> x∈X

F (x) = ⟨m, M f (x)⟩M, (1) where f : X → Y is an unknown vector-valued operator, Y is the output Hilbert space. The operator 

M : Y → M is a known bounded linear measurement operator, where M is the Hilbert space of measurements, and m ∈ M defines a linear functional that evaluates the measured output. For each input x, the output f (x) is not directly observable; instead, only its measurement M f (x) is available. 

Figure 1: Vector-valued BO framework. Red box: classical BO framework. Blue box: vector-valued BO framework introduced in this work. 

Practical implication. This formulation gen-eralizes different observation regimes, ranging from full observation ( M = I) to scalar ob-servation ( M is a linear functional). The inner product ⟨m, M f (x)⟩M naturally arises in many applications where the objective depends on vector-valued outputs linearly, e.g., energy con-sumption computed from trajectories in build-ing control [ 44 , 39 ]. It captures a broad class of common operations such as point evaluations, integrals, and averaging. Examples of common linear functionals in different Hilbert spaces are provided in Appendix A. 

Comparison to scalar-valued BO. As illus-trated in Fig. 1, scalar-valued BO models an end-to-end mapping from x to F (x) using scalar feedback, thereby discarding the underlying structure in f (x). This limits the ability to ex-ploit richer observations and prevents informa-tion transfer across different objectives. In contrast, our formulation explicitly incorporates structured measurements M f (x), enabling more informative learning and adaptation across objectives. 

Comparison to BO with augmented input. Observe that for any m and M , ⟨m, M f (x)⟩M =

⟨M ∗m, f (x)⟩Y . Thus, the objective can be viewed as applying a linear functional M ∗m ∈ Y to f (x).A common approach is to model this quantity using a scalar-valued function ˜F (x, y ) 7 → ⟨ y, f (x)⟩Y

with an augmented input space X × Y , which is learned via a scalar kernel [ 30 , 20 ]. While this enables standard scalar-valued analysis, it reduces the observation to a scalar projection of the output. In contrast, our formulation provides flexibility. Different choices of M correspond to different observation mechanisms, which can significantly affect the amount of information obtained on f (x).

Performance metric. Since f is a black-box operator, solving (1) in a single step is generally intractable. Instead, we aim to construct a sequence of inputs {xt}Tt=1 under a finite budget T

such that the resulting averaged performance approximates the optimal solution of (1) . Specifically, at each iteration t, the learner queries f at a chosen point xt and observes noisy measurements 

yt = M f (xt) + δt, where δt ∈ M is additive noise. The goal is to design a policy that balances exploration and exploitation, which can be quantified via the cumulative regret 

RT =

> T

X

> t=1

⟨m, M f (x∗) − M f (xt)⟩M,

where x∗ denotes an optimal solution to (1) . Sublinear growth of RT with respect to T indicates the problem is effectively solved [ 40 , 10 ]. In general, such results are not attainable for arbitrary operators and noise processes. To obtain meaningful results, we impose the following assumptions. 

Assumption 1 (Observation model) . Conditioned on the natural filtration Ft−1, δt ∈ M is a zero-mean sub-Gaussian random element, i.e., ∃σ > 0, ∀α ∈ M , E[e⟨α,δ t⟩M |F t−1] ≤ eσ2∥α∥2

> M/2

.

Remark 1. The sub-Gaussian noise assumption is commonly adopted in the literature [ 11 , 20 ]. It ensures that the noise has controlled (light-tailed) behavior. Depending on the measurement space 

3M, the noise δ can take values in an infinite-dimensional Hilbert space. The sub-Gaussian property implies that the noise distribution cannot spread uniformly across infinitely many directions. Instead, its magnitude must decay sufficiently fast along higher-order directions. 

Vector-valued RKHS. Vector-valued RKHS is a direct generalization of scalar-valued RKHS [ 33 ]and contains the operators with outputs in Hilbert space Y. We adopt its definition from [33]. 

Definition 1. The Hilbert space H is a reproducing kernel Hilbert space if for any y ∈ Y and x ∈ X ,the linear functional that maps f ∈ H to ⟨y, f (x)⟩Y is continuous. 

According to the Riesz representation theorem [ 1], we can define a mapping Φ : X → L (Y, H),where L(Y, H) is the Banach space of bounded linear operator, such that for all y ∈ Y , ⟨y, f (x)⟩Y =

⟨Φ( x)y, f ⟩H. Consequently, a mapping K : X × X → L (Y) defined by K(x, s )y = (Φ( x)y)( s) is called a vector-valued kernel. Further properties of such a kernel can be found in [6, 8]. 

Assumption 2 (Regularity) . The unknown operator f belongs to the vector-valued RKHS H, induced by the operator-valued kernel K. The RKHS norm of f is bounded as ∥f ∥H ≤ Γ. Furthermore, we assume that the kernel is uniformly bounded, i.e., 

sup 

> x∈X

∥K(x, x )∥op ≤ CK .

We further assume that given a pairwise distinct input set Xt := {xi}ti=1 , any operator-valued matrix 

KXtXt with its elements given by (KXtXt )i,j = K(xi, x j ) is a trace-class operator, i.e. the sum of eigenvalues (point spectrum) of KXtXt is finite. 

Remark 2. The boundedness of the kernel and the RKHS norm ensures that the target operators lie in a well-structured hypothesis space, enabling efficient uncertainty reduction through sequential sampling. The trace-class assumption on the operator-valued kernel is essential for deriving theoreti-cal guarantees, as it ensures that the information gained from the samples is finite. This condition is mild and satisfied by a broad class of kernels, such as separable kernels [20, 11]. 

Frequentist perspective In this work, we adopt a frequentist perspective and assume that the unknown operator is fixed with bounded norm. The only source of randomness arises from the observation noise. Accordingly, we analyze worst-case regret over all possible operators, whereas the Bayesian formulation considers expected regret under a prior distribution. 

Extension to multiple and time-varying objectives. While we focus on the formulation in (1) with a fixed objective for clarity of analysis, the proposed framework naturally extends to settings where the objective varies across iterations, e.g., Ft(x) = ⟨mt, M f (x)⟩. In particular, by quantifying the structured measurement M f (x), the resulting confidence sets can be transferred across different objectives defined by different linear functionals. This enables efficient adaptation to multiple objectives and time-varying settings. We defer a detailed discussion to Appendix G. 

## 3 Confidence bound in measurement space 

Vector-valued KRR. Let Xt := {xi}ti=1 be a set of pairwise distinct inputs and Yt := (y1, . . . , y t) ∈ M t the corresponding noisy observations, where yi = M f (xi) + δi, and Mt is the direct sum of t copies of M. An estimator μt ∈ H of f is obtained by solving the regularized risk minimization problem 

μt = arg min  

> ˜f∈H
> t

X

> i=1

∥M ˜f (xi) − yi∥2 

> M

+ λ∥ ˜f ∥2

> H

, (2) where λ > 0 is a regularization parameter controlling the complexity of the estimator. The explicit formulation of μt is provided in Appendix B. To analyze the prediction error ∥M f (x) − M μ t(x)∥M,a natural approach is to first bound the error in the output space Y and then propagate it through the measurement operator M . However, this can be overly conservative, as it requires controlling errors in all directions of Y, including those that are not observable through M . This observation motivates modeling the measurable quantity M f (x) directly in the measurement space. 4Induced kernel in the measurement space. We introduce the induced kernel KM : X × X → L(M) defined by 

KM (x, s ) = M K (x, s )M ∗,

which is an operator-valued kernel taking values in L(M). The following lemma shows that KM is a reproducing kernel in the measurement space. 

Lemma 1 (Proposition 6, [ 8 ]) . Let M ∈ L (Y, M) and KM (x, s ) = M K (x, s )M ∗. Then KM is a reproducing kernel, and the corresponding RKHS HM is continuously embedded in H.

Lemma 1 shows that composing elements of a vector-valued RKHS with a bounded linear operator yields a new vector-valued RKHS with kernel KM . Consequently, the induced estimator in the measurement space can equivalently be obtained by performing KRR in HM using the observations 

Yt [24, 33]. Specifically, for any x ∈ X , the estimator of M f (x) in M is given by 

KMxX t

 KMXtXt + λI −1Yt, (3) where KMxX t ∈ L (Mt, M) is defined by KMxX t v = Pti=1 KM (x, x i)vi, ∀v = ( v1, . . . , v t) ∈ M t,and KMXtXt ∈ L (Mt, Mt) is the block operator matrix with entries (KMXtXt )ij = KM (xi, x j ).Note that KMXtXt is a trace-class operator since KXtXt has finite trace. Importantly, (3) coincides with the measured output of the solution to (2) , i.e., M μ t(x) (see Appendix B). Thus, working in the measurement space does not change the estimator for the observable quantity M f (x), but provides a straightforward formulation. In particular, it enables uncertainty quantification directly in M,avoiding the conservatism introduced by bounding errors in the full output space Y.

Concentration bound. To quantify the uncertainty of the estimator, we define 

KMt (x, x ) = KM (x, x ) − KMxX t

 KMXtXt + λI −1KMXtx,

where KMXtx is the adjoint of KMxX t . This operator can be interpreted as the posterior variance, which captures the remaining uncertainty at x after observing Yt. Using this characterization, we can derive high-probability confidence bounds for the prediction error in the measurement space. 

Theorem 1. Let Assumptions 1-2 hold. Assuming that KMXtXt is a trace-class operator, given Xt

and Yt, with probability at least 1 − ζ, for any x ∈ X , the prediction error is bounded by 

∥M f (x)−M μ t(x)∥M ≤



Γ+ σ

√λ

q

2 log(1 /ζ )+log det  I +λ−1KMXtXt



∥KMt (x, x )∥1/2op .

The proof of Theorem 1 can be found in Appendix C. The bound consists of two components. The first term, log det  I + λ−1KMXtXt

, resembles the mutual information in the scalar-valued setting. Assuming that KMXtXt is a trace-class operator, i.e., the sum of its eigenvalues is finite, ensures that the Fredholm determinant is well-defined and finite. The second term, ∥KMt (x, x )∥1/2op , captures the uncertainty at the query point x. In practice, this quantity can be evaluated via the largest eigenvalue of KMt (x, x ). This upper bound exhibits a structure similar to the bounds in [ 10 , 11 ]. In fact, it can be viewed as a direct generalization of these results, extending the measurement space from a finite-dimensional space to a possibly infinite-dimensional Hilbert space. 

3.1 Special cases Full observation ( M = I). When M = I, the learner observes the full vector-valued output f (x),and the induced kernel reduces to KM (x, s ) = K(x, s ). The objective becomes ⟨m, f (x)⟩Y with 

m ∈ Y . This corresponds to the most informative measurements, where the entire output is observed. In this case, the confidence bound in Theorem 1 provides uncertainty quantification directly over the vector-valued output f (x).

Scalar linear measurements ( M : Y → R). When M is a bounded linear functional, the obser-vations become scalar-valued. In this case, KM (x, s ) reduces to a scalar kernel, and the problem recovers standard scalar-valued Bayesian optimization. This regime corresponds to minimal informa-tion acquisition, where each query reveals only a one-dimensional projection of f (x).These cases show that our framework unifies both full vector-valued observation and scalar-valued observation as special instances of a general formulation. Intermediate choices of M interpolate between these two cases, providing a principled way to model the trade-off between observation richness and algorithm efficiency. 5Algorithm 1 Vector-valued Bayesian optimization (vvBO) 

Require: Total budget T , regularization parameter λ > 0, confidence level ζ ∈ (0 , 1)  

> 1:

Initialize dataset X0 = ∅, Y0 = ∅ with M μ 0(x) = 0 , KM 

> 0

(x, x ) = KM (x, x ) 

> 2:

for t = 1 , . . . , T do  

> 3:

Compute M μ t−1(x), KMt−1(x, x ) 

> 4:

Choose the next sample via 

xt ∈ arg max  

> x∈X

⟨m, M μ t−1(x)⟩M + βt−1∥m∥M∥KMt−1(x, x )∥1/2op  

> 5:

Query f at xt and observe yt = M f (xt) + δt 

> 6:

Update the dataset Xt, Yt 

> 7:

end for 

## 4 Algorithm and regret analysis 

We construct an upper and lower bound for F (x) at any iteration t ≥ 1. In particular, given dataset 

Xt−1, Yt−1, F (x) ∈ [F t−1(x), F t−1(x)] , where 

F t−1(x) = ⟨m, M μ t−1(x)⟩M + ∥m∥M ∥M f (x) − M μ t−1(x)∥M,F t−1(x) = ⟨m, M μ t−1(x)⟩M − ∥ m∥M ∥M f (x) − M μ t−1(x)∥M.

The deviation term ∥M f (x)−M μ t−1(x)∥M can be bounded using Theorem 1, yielding a computable confidence interval. To solve problem (1) , we adopt the principle of optimism in the face of uncertainty, leading to a UCB-based algorithm as described in Algorithm 1. 

Algorithm. We are now ready to present the vector-valued Bayesian Optimization (vvBO) 

algorithm with structured measurements. We define the confidence parameter as βt−1 =Γ + σ√λ

q

2 log(1 /ζ ) + log det  I + λ−1KMXt−1Xt−1

. At each iteration t, the learner constructs a KRR estimator in the measurement space using the dataset Xt−1, Y t−1, and forms an up-per confidence bound based on Theorem 1. The next query point is selected as xt =arg max x∈X ⟨m, M μ t−1(x)⟩M + βt−1∥m∥M∥KMt−1(x, x )∥1/2op , which balances exploration and exploitation through the confidence parameter βt−1. The observation is then obtained as yt =

M f (xt) + δt. Notably, the entire procedure operates directly in the measurement space, leveraging structured observations through the induced kernel KM .

4.1 Regret analysis 

The following theorem establishes a general regret bound for Algorithm 1. 

Theorem 2. Let Assumptions 1–2 hold. For any λ > 0 and ζ ∈ (0 , 1) , with probability at least 1 − ζ,the regret of Algorithm 1 satisfies 

RT ≤ 2∥m∥M



Γ+ σ

√λ

r

2 log( 1

ζ ) + log det  I +λ−1KMXT XT

 vuutT

> T

X

> t=1

∥KMt−1(xt, x t)∥op .

The proof of Theorem 2 is deferred to Appendix D. The resulting bound shares a similar structure with classical results in scalar-valued Bayesian optimization [ 10 , 40 ], as well as extensions to finite-dimensional vector-valued settings [ 11 ]. Despite this similarity, establishing sublinear regret in our setting is nontrivial. The term log det( I + λ−1KMXT XT ) involves an operator-valued kernel and is well-defined only when KMXT XT is trace-class. However, this condition alone does not characterize how the quantity grows with T . Likewise, boundedness of ∥KMt−1(xt, x t)∥op does not control the cumulative growth of the uncertainty terms. These challenges motivate restricting attention to structured kernel classes, under which explicit bounds can be derived. 

Separable kernel. We restrict attention to a widely used class of separable kernels [ 8]. Specifically, we consider kernels of the form K = G⊗B, where G : X ×X → R is a scalar-valued kernel and B ∈

6L(Y) is a positive trace-class operator. Under a measurement operator M , this induces KM (x, s ) = 

G(x, s ) ( M BM ∗) ∈ L (M). The trace-class property is preserved under the composition, i.e., 

M BM ∗ remains trace-class for bounded M . We write BM = M BM ∗ ∈ L (M) and 

KMXtXt = GXtXt ⊗ BM , KMxX t = GxX t ⊗ BM , KMXtx = GXtx ⊗ BM ,

where GXtXt ∈ Rt×t is the kernel Gram matrix and GxX t ∈ R1×t, GXtx ∈ Rt×1.

Theorem 3 (Kernel specific bound) . Let Assumptions 1-2 hold. For the separable kernel with BM

having finite spectrum, if X ⊂ Rd, with probability at least 1 − ζ, the regret for Algorithm 1 satisfies: 1. Linear kernel: RT ≤ O  log T √T ,2. Gaussian kernel: RT ≤ O  (log T )d+1 √T ),3. Matérn kernel: RT ≤ O  T d(d+1) /(2 ν+d(d+1)) log T √T .

The proof of Theorem 3 is deferred to Appendix E. The above results show that, with separable kernels, the proposed method achieves sublinear regret rates comparable to those in classical scalar-valued Bayesian optimization. In particular, the dependence on T matches known results despite the system outputs being vector-valued elements in Hilbert spaces. These guarantees follow from combining the high-probability confidence bounds in Theorem 1 with the information-gain bounds used in the proof of Theorem 3, demonstrating that efficient sequential optimization is achievable under general measurement operators. 

Extension to time-varying and nonlinear functions. As discussed in Section 2, the proposed algorithm readily extends to time-varying objectives with time-dependent linear functionals mt.Details are provided in Appendix G. Furthermore, the results in this work can also be naturally generalized to any nonlinear function F ′ : M → R satisfying a Lipschitz condition. A detailed discussion is deferred to Appendix G. 

Efficient computation. Note that for infinite-dimensional output spaces with general operator-valued kernels, operations such as inner products and norms can be computationally intractable. We provide explicit constructions for separable kernels in the appendix to enable the practical implementation of the proposed bounds and algorithm. The key idea is to exploit the spectral decomposition of the operator-valued kernel and truncate the resulting infinite series at a finite level. This yields a finite-dimensional approximation. A detailed formulation can be found in Appendix H. 

## 5 Numerical experiments 

In this section, we demonstrate that leveraging structured measurements improves efficiency in optimizing black-box systems with vector-valued outputs via several numerical experiments, par-ticularly with changing objectives. We compare the proposed vector-valued Bayesian optimization (vvBO) algorithm against multiple baselines, including vanilla BO [ 10 ], multi-task BO (MTBO) [ 11 ], function-on-function BO (FFBO) [ 20 ], and contextual BO (CTBO), where the context is defined as 

M ∗m in the augmented input space (see details in Appendix I.1). In all experiments, the underlying system produces structured outputs (e.g., trajectories or functions), and the objective is defined as a linear functional of these outputs. We consider both synthetic benchmarks and a real-world controller tuning task. All results are averaged over 10 independent runs with different initial starting points without pretraining. Simulation details, including baseline implementation, hyperparameters, and computational time, are provided in Appendix I.1. 

Synthetic problems. Here, we explore how structured measurements help to adapt to different objectives. We consider the setting where M = I for vvBO, i.e., measuring the full vector-valued outputs. Additional results for partial observations ( M̸ = I) are provided in Appendix I.2. The optimization process is divided into three phases, each corresponding to a different objective defined by mi, i ∈ { 1, 2, 3} (see detailed formulation in Appendix I.2). All the methods retain and reuse all previously collected samples across phases. Additionally, we also compare against vanilla BO (rBO) and multi-task BO (rMTBO) re-initialized when shifting to the next phase. Performance is evaluated using both simple and cumulative regret. The results for three test functions are shown in Fig. 2. Across all experiments, vvBO with full vector-valued measurement consistently outperforms the baseline methods for different objectives. Since most baseline methods operate on scalar observations 70                    

> 2
> 4
> Phase one Phase two Phase three
> GP
> Simple regret
> 0
> 50
> 100
> Phase one Phase two Phase three
> Cumulative regret
> 0
> 20
> 40
> 60
> Ackley
> 0
> 1000
> 2000
> 020 40 60 80 100
> Iteration
> 0
> 200
> 400
> 600
> Eggholder
> 020 40 60 80 100
> Iteration
> 0
> 10000
> 20000
> vvBO (ours) CTBO MTBO [11] rMTBO [11] BO rBO FFBO [20]

Figure 2: Comparison of simple regret and cumulative regret for three test operators with different baseline methods. vvBO with full trajectory measurements has the lowest regret in all scenarios, which indicates that it efficiently accommodates to different objectives. or fixed finite-dimensional representations, they cannot fully exploit the structure of the output or adjust effectively to changes in the objective. In contrast, our approach models the underlying vector-valued structure, facilitating effective reuse of information and rapid tracking. As for CTBO, while capable of handling varying objectives, it incurs larger cumulative regret since for each new context, it requires a large amount of exploration before establishing an accurate model. A detailed comparison between vvBO and CTBO is provided in Appendix I.2. 

Time-varying objectives in real-world controller tuning. We next evaluate vvBO in a more realistic setting with time-varying objectives. We consider a real-world problem of tuning a Model Predictive Control (MPC) controller, which regulates indoor temperature in a commercial building testbed [ 47 ]. The simulated building has a surface area of 8500 m 2 and serves approximately 1350 occupants, with heating provided by a district heating network. To control the temperature, the MPC controller regulates the valve position of a radiator and is parameterized by three variables. The goal is to choose the optimal parameter that minimizes a weighted sum of energy consumption, CO 2

emissions, and thermal discomfort. This economic objective is commonly found in building control, which captures the trade-off between energy efficiency and thermal comfort [ 9]. To create realistic conditions, we introduce a time-varying heating price following a sinusoidal pattern, resulting in a sequence of time-varying objectives (see detailed formulation in Appendix I.3). Controller parameters are updated daily, and temperature and heating trajectories are continuously measured throughout each day. Experiments are conducted over 200 iterations with varying initial conditions. The first row of Fig. 3 presents the cost during the active learning stage. After 200 iterations, vvBO achieves the lowest cost among all baseline methods. The impact of the time-varying heating price is reflected in the left plot. In the early phase, vvBO behaves similarly to baseline methods, focusing on exploration. As more data are collected, it adapts effectively to the varying prices by reducing the temperature when prices are high to save energy (peaks), and increasing the temperature when prices are low to improve comfort (valleys). Consequently, after around 150 iterations, vvBO significantly reduces the peak cost. Details on how the heating price affects the total cost can be found in Appendix I.3. This reveals the ability of vvBO to respond to time-varying objectives through rich measurements. In the second row of Fig. 3, the validation of the learned model is illustrated. In the left plot, we vary the heating price and evaluate the economic cost associated with the recommended parameter from each method. vvBO achieves the lowest cost across all price levels, while other baseline methods incur 80 50 100 150 200                 

> Iteration
> 0
> 1
> 2
> Relative cost  vvBO reduces peak cost
> Active learning
> 050 100 150 200
> Iteration
> 450
> 500
> 550
> 600
> Running average cost [EUR]
> Active learning
> 0.00 0.05 0.10 0.15 0.20
> Heating price [EUR/kWh]
> 200
> 400
> 600
> 800
> Optimal cost [EUR]
> CTBO
> Validation
> 00:00 06:00 12:00 18:00 24:00
> Time of day
> 0
> 100
> 200
> Optimal power [kW]  Validation
> vvBO (ours) CTBO MTBO [11] BO FFBO [20]

Figure 3: Real-world tuning of an MPC controller for building climate control. high costs. In the right plot, we consider a new objective that is commonly found in building control [ 17 , 26 ], minimizing the peak heating power at specific times of the day. This objective is never encountered during the active learning stage, which evaluates the ability to transfer information across different objectives. vvBO continues to recommend effective parameters across all time instances, whereas CTBO, which achieves comparable performance for the economic objective, performs poorly due to large uncertainty in unseen contexts. Indeed, it requires substantially more data to learn the new context. These results highlight that, by leveraging structured measurements, vvBO can generalize across objectives without modeling changing components, allowing fast accommodation in time-varying and previously unseen settings. 

## 6 Conclusion and limitations 

In this work, we study Bayesian optimization for systems that produce structured, vector-valued outputs observed through linear measurement operators. By modeling directly in the measurement space, we derive general concentration bounds for vector-valued KRR in the measurement space. Building on these results, we propose a UCB-based algorithm and establish sublinear regret guarantees for common kernels. This shows that effective learning is also possible when measurements lie in a general Hilbert space. Furthermore, our work highlights the importance of the observation model in Bayesian optimization: when structured measurements are available, each measurement can reveal substantially richer information about the underlying system than scalar observations. Leveraging this structure can facilitate more efficient learning and information sharing across multiple objectives. Numerical experiments validate these benefits in both synthetic and real-world scenarios. While the proposed framework provides a general approach to Bayesian optimization with structured measurements, it has several limitations. First, similar to standard BO methods, performance may degrade in high-dimensional input spaces due to the curse of dimensionality. Second, our analysis focuses on unconstrained settings, whereas many applications impose constraints on trajectories or structured outputs [ 43 , 44 , 9]. Extending the framework to incorporate constraints and safety considerations is an important direction for future work. Regarding social impact, the proposed framework has the potential to improve decision-making in applications involving complex dynamical systems, such as energy management, climate control, and environmental monitoring, by enabling more efficient use of data and better utilization of structured information. However, as with many data-driven optimization methods, care must be taken to ensure robustness, privacy, fairness, and reliability when deployed in real-world systems. 9References 

[1] Naum Ilich Akhiezer. Theory of linear operators in Hilbert space . Pitman Publishing, 1981. [2] David Blum, Javier Arroyo, Sen Huang, Ján Drgoˇ na, Filip Jorissen, Harald Taxt Walnum, Yan Chen, Kyle Benne, Draguna Vrabie, Michael Wetter, et al. Building optimization testing framework (BOPTEST) for simulation-based benchmarking of control strategies in buildings. Journal of Building Performance Simulation , 14(5):586–610, 2021. [3] Edwin V Bonilla, Kian Chai, and Christopher Williams. Multi-task Gaussian process prediction. Advances in Neural Information Processing Systems , 20, 2007. [4] Romain Brault, Alex Lambert, Zoltán Szabó, Maxime Sangnier, and Florence d’Alché Buc. Infinite task learning in RKHSs. In The 22nd International Conference on Artificial Intelligence and Statistics , pages 1294–1302. PMLR, 2019. [5] Paul Brunzema, Alexander Von Rohr, and Sebastian Trimpe. On controller tuning with time-varying Bayesian optimization. In 2022 IEEE 61st Conference on Decision and Control , pages 4046–4052. IEEE, 2022. [6] Andrea Caponnetto, Charles A Micchelli, Massimiliano Pontil, and Yiming Ying. Universal multi-task kernels. The Journal of Machine Learning Research , 9:1615–1646, 2008. [7] Claudio Carmeli, Ernesto De Vito, and Alessandro Toigo. Vector valued reproducing kernel Hilbert spaces of integrable functions and Mercer theorem. Analysis and Applications , 4(04):377–408, 2006. [8] Claudio Carmeli, Ernesto De Vito, Alessandro Toigo, and Veronica Umanitá. Vector valued reproducing kernel Hilbert spaces and universality. Analysis and Applications , 8(01):19–61, 2010. [9] Xiao Chen, Qian Wang, and Jelena Srebric. Model predictive control for indoor thermal comfort and energy optimization using occupant feedback. Energy and Buildings , 102:357–369, 2015. [10] Sayak Ray Chowdhury and Aditya Gopalan. On kernelized multi-armed bandits. In International Conference on Machine Learning , pages 844–853. PMLR, 2017. [11] Sayak Ray Chowdhury and Aditya Gopalan. No-regret algorithms for multi-task Bayesian optimization. In International Conference on Artificial Intelligence and Statistics , pages 1873–1881. PMLR, 2021. [12] João PL Coutinho, You Peng, Ricardo Rendall, Kaiwen Ma, Swee-Teng Chin, Ivan Castillo, and Marco S Reis. Efficient human-in-the-loop MPC tuning with multi-task preferential Bayesian optimization. Control Engineering Practice , 173:106972, 2026. [13] Samuel Daulton, David Eriksson, Maximilian Balandat, and Eytan Bakshy. Multi-objective Bayesian optimization over high-dimensional search spaces. In Uncertainty in Artificial Intelligence , pages 507–517. PMLR, 2022. [14] Theodoros Evgeniou and Massimiliano Pontil. Regularized multi–task learning. In Proceedings of the 10th ACM SIGKDD International Conference on Knowledge Discovery and Data Mining , pages 109–117, 2004. [15] Peter I Frazier. A tutorial on Bayesian optimization. arXiv preprint arXiv:1807.02811 , 2018. [16] Roman Garnett. Bayesian optimization . Cambridge University Press, 2023. [17] Linas Gelazanskas and Kelum A.A. Gamage. Demand side management in smart grid: A review and proposals for future direction. Sustainable Cities and Society , 11:22–30, 2014. [18] Jan Gertheiss, David Rügamer, Bernard XW Liew, and Sonja Greven. Functional data analysis: An introduction and recent developments. Biometrical Journal , 66(7):e202300363, 2024. [19] Henrik Hose, Paul Brunzema, Alexander Von Rohr, Alexander Gräfe, Angela P Schoellig, and Sebastian Trimpe. Fine-tuning of neural network approximate MPC without retraining via Bayesian optimization. 

arXiv preprint arXiv:2512.14350 , 2025. [20] Jingru Huang, Haijie Xu, Manrui Jiang, and Chen Zhang. Function-on-function Bayesian optimization. In 

Proceedings of the AAAI Conference on Artificial Intelligence , volume 40, pages 21994–22002, 2026. [21] Hiroshi Isozaki and Evgeny L. Korotyaev. Trace formulas for Schrödinger operators – from the view point of complex analysis. In RIMS Kôkyûroku , pages 16–32, 2011. 

10 [22] Hachem Kadri, Emmanuel Duflos, Manuel Davy, Philippe Preux, and Stephane Canu. General framework for nonlinear functional regression with reproducing kernel Hilbert spaces . PhD thesis, INRIA, 2009. [23] Hachem Kadri, Emmanuel Duflos, Philippe Preux, Stéphane Canu, and Manuel Davy. Nonlinear functional regression: a functional RKHS approach. In Proceedings of the Thirteenth International Conference on Artificial Intelligence and Statistics , pages 374–380. JMLR Workshop and Conference Proceedings, 2010. [24] Hachem Kadri, Emmanuel Duflos, Philippe Preux, Stéphane Canu, Alain Rakotomamonjy, and Julien Audiffren. Operator-valued kernels for learning from functional response data. Journal of Machine Learning Research , 17(20):1–54, 2016. [25] Nazan Khan, David E Goldberg, and Martin Pelikan. Multi-objective Bayesian optimization algorithm. In 

Proceedings of the 4th Annual Conference on Genetic and Evolutionary Computation , pages 684–684, 2002. [26] Anna Magdalena Kosek, Giuseppe Tommaso Costanzo, Henrik W Bindner, and Oliver Gehrke. An overview of demand side management control schemes for buildings in smart grids. In 2013 IEEE International Conference on Smart Energy Grid Engineering , pages 1–9. IEEE, 2013. [27] Nikola Kovachki, Zongyi Li, Burigede Liu, Kamyar Azizzadenesheli, Kaushik Bhattacharya, Andrew Stuart, and Anima Anandkumar. Neural operator: Learning maps between function spaces with applications to PDEs. Journal of Machine Learning Research , 24(89):1–97, 2023. [28] Andreas Krause and Cheng Ong. Contextual Gaussian process bandit optimization. Advances in Neural Information Processing Systems , 24, 2011. [29] Akshay Kudva and Joel A. Paulson. Bonsai: Structure-exploiting robust Bayesian optimization for networked black-box systems under uncertainty. Computers & Chemical Engineering , 204:109393, Jan 2026. [30] Amon Lahr, Anna Scampicchio, Johannes Köhler, and Melanie N Zeilinger. Optimal uncertainty bounds for multivariate kernel regression under bounded noise: A Gaussian process-based dual function. arXiv preprint arXiv:2603.16481 , 2026. [31] Zongyi Li, Nikola Kovachki, Kamyar Azizzadenesheli, Burigede Liu, Kaushik Bhattacharya, Andrew Stuart, and Anima Anandkumar. Neural operator: Graph kernel network for partial differential equations. 

arXiv preprint arXiv:2003.03485 , 2020. [32] Johanna Menn, David Stenger, and Sebastian Trimpe. Preferential Bayesian optimization with crash feedback. IEEE Robotics and Automation Letters , 2026. [33] Charles A Micchelli and Massimiliano Pontil. On learning vector-valued functions. Neural computation ,17(1):177–204, 2005. [34] Charles A Micchelli, Yuesheng Xu, and Haizhang Zhang. Universal kernels. Journal of Machine Learning Research , 7(12), 2006. [35] Ha Quang Minh. Infinite-dimensional log-determinant divergences between positive definite trace class operators. Linear Algebra and Its Applications , 528:331–383, 2017. [36] James O Ramsay and Bernard W Silverman. Functional data analysis . Springer, 1997. [37] Carlos Ruiz, Carlos M Alaíz, and José R Dorronsoro. A survey on kernel-based multi-task learning. 

Neurocomputing , 577:127255, 2024. [38] Pier Giuseppe Sessa, Pierre Laforgue, Nicolò Cesa-Bianchi, and Andreas Krause. Multitask learning with no regret: from improved confidence bounds to active learning. Advances in Neural Information Processing Systems , 36:6770–6781, 2023. [39] Jicheng Shi, Christophe Salzmann, and Colin N Jones. Disturbance-adaptive data-driven predictive control: Trading comfort violation for savings in building climate control. IEEE Transactions on Control Systems Technology , 2026. [40] Niranjan Srinivas, Andreas Krause, Sham M Kakade, and Matthias W Seeger. Information-theoretic regret bounds for Gaussian process optimization in the bandit setting. IEEE Transactions on Information Theory ,58(5):3250–3265, 2012. [41] Kevin Swersky, Jasper Snoek, and Ryan P Adams. Multi-task Bayesian optimization. Advances in Neural Information Processing Systems , 26, 2013. 

11 [42] Ngo Anh Vien, Heiko Zimmermann, and Marc Toussaint. Bayesian functional optimization. In Proceedings of the AAAI Conference on Artificial Intelligence , volume 32, 2018. [43] Wenbin Wang, Zhiyu He, Giuseppe Belgioioso, Saverio Bolognani, and Florian Dorfler. Decentralized feedback optimization via sensitivity decoupling: Stability and sub-optimality. In 2024 European Control Conference , pages 3201–3206. IEEE, 2024. [44] Wenbin Wang, Jicheng Shi, and Colin N Jones. Personalized building climate control with contextual preferential Bayesian optimization. arXiv preprint arXiv:2512.09481 , 2025. [45] Wenjie Xu, Bratislav Svetozarevic, Loris Di Natale, Philipp Heer, and Colin N Jones. Data-driven adaptive building thermal controller tuning with constraints: A primal–dual contextual Bayesian optimization approach. Applied Energy , 358:122493, 2024. [46] Wenjie Xu, Bratislav Svetozarevic, Loris Di Natale, Philipp Heer, and Colin N. Jones. Data-driven adaptive building thermal controller tuning with constraints: A primal–dual contextual Bayesian optimization approach. Applied Energy , 358:122493, 2024. [47] Tao Yang, Konstantin Filonenko, Krzysztof Arendt, and Christain Veje. Implementation and performance analysis of a multi-energy building emulator. In 2020 6th IEEE International Energy Conference , pages 451–456. IEEE, 2020. [48] Jiarui Yu, Jicheng Shi, Wenjie Xu, and Colin N Jones. Which price to pay? auto-tuning building mpc controller for optimal economic cost. arXiv preprint arXiv:2501.10859 , 2025. [49] Ming Yuan and T. Tony Cai. A reproducing kernel hilbert space approach to functional linear regression. 

The Annals of Statistics , 38(6), December 2010. 

## A Common linear functionals 

In this section, we provide examples of linear functionals in common Hilbert spaces. In all cases, continuous linear functionals admit a representation via the inner product, as characterized by the Riesz representation theorem [1]. 

Finite-dimensional spaces. In Rn with inner product 

⟨x, y ⟩ =

> n

X

> i=1

xiyi,

every linear functional φ can be written as 

φ(x) = 

> n

X

> i=1

aixi = ⟨x, y ⟩,

where y = ( a1, . . . , a n) ∈ Rn.

Sequence space. The Hilbert space ℓ2 consists of square-summable sequences with inner product 

⟨x, y ⟩ =

> ∞

X

> k=1

xkyk.

Every continuous linear functional φ admits the representation 

φ(x) = 

> ∞

X

> k=1

ckxk = ⟨x, y ⟩,

for some y = {ck}∞ 

> k=1

∈ ℓ2.12 Square-integrable function spaces. Let (Ω , μ ) be a measure space. The space L2(Ω , μ ) has inner product 

⟨f, g ⟩ =

Z

> Ω

f (x)g(x) dμ (x).

By the Riesz representation theorem, every continuous linear functional φ can be expressed as 

φ(f ) = 

Z

> Ω

f (x)g(x) dμ (x) = ⟨f, g ⟩,

for a unique g ∈ L2(Ω , μ ).

Sobolev spaces. Sobolev spaces are Hilbert spaces equipped with inner products involving deriva-tives. For example, in H1(Ω) ,

⟨u, v ⟩ =

Z

> Ω

∇u · ∇ v + uv dx. 

Every continuous linear functional φ admits a representation 

φ(u) = ⟨u, h ⟩H1 ,

for a unique h ∈ H1(Ω) .

Reproducing kernel Hilbert spaces. Let H be a scalar-valued RKHS with kernel G : X × X → R.By the reproducing property, 

f (x) = ⟨f, G (x, ·)⟩H.

The evaluation functional L(f ) = f (x0) is represented by G(x0, ·). More generally, for 

L(f ) = 

> n

X

> i=1

aif (xi),

the representer is 

y =

> n

X

> i=1

aiG(xi, ·).

Integral functionals can also be expressed in this form. For example, let 

L(f ) = 

Z

> X

g(x)f (x) dx, 

where the integral is well-defined, and the resulting functional is continuous. Using the reproducing property, 

L(f ) = 

Z

> X

g(x)⟨f, G (x, ·)⟩Hdx =



f, 

Z

> X

g(x)G(x, ·) dx 



> H

.

Thus, the representer is 

y =

Z

> X

g(x)G(x, ·) dx ∈ H .

For two such functionals with weights g1 and g2, their inner product is 

⟨y1, y 2⟩H =

Z

> X

Z

> X

g1(x)g2(y)G(x, y ) dx dy. 

More generally, for 

L(f ) = 

> n

X

> i=1

ai

Z

> X

gi(x)f (x) dx, 

the corresponding representer is 

y =

> n

X

> i=1

ai

Z

> X

gi(x)G(x, ·) dx. 

13 B Analytical formulation for the vector-valued estimator 

In the following lemma, we provide the explicit formulation for (2). 

Lemma 2. Let Assumptions 1-2 hold. For any x ∈ X , μt(x) is given by 

μt(x) = 

> t

X

> i=1

K(x, x i)M ∗αi,

where αi ∈ M solves a linear system of equations, which is given by 

> t

X

> j=1

(M K (xi, x j )M ∗ + λI i=j )αj = yi,

where Ii=j is the indicator operator, i.e., Ii=j = I if i = j and Ii=j = 0 otherwise. Proof. For any f ∈ H , we can express it as f = μt + g, where g ∈ H represents the residual. Note that 

> t

X

> i=1

∥M f (xi)−yi∥2 

> M

+ λ∥f ∥2

> H

=

> t

X

> i=1

∥M μ t(xi) + M g (xi) − yi∥2 

> M

+ λ∥μt + g∥2

> H

=

> t

X

> i=1

∥M g (xi)∥2 

> M

+

> t

X

> i=1

∥M μ t(xi) − yi∥2 

> M

− 2

> t

X

> i=1

⟨M g (xi), y i − M μ t(xi)⟩M

+ λ∥μt∥2 

> H

+ λ∥g∥2 

> H

+ 2 λ⟨μt, g ⟩H

=

> t

X

> i=1

∥M μ t(xi) − yi∥2 

> M

+ λ∥μt∥2

> H

+

> t

X

> i=1

∥M g (xi)∥2 

> M

+ λ∥g∥2

> H

+ 2 λ⟨μt, g ⟩H − 2

> t

X

> i=1

⟨M g (xi), y i − M μ t(xi)⟩M.

For the last two term, we have 

λ⟨μt, g ⟩H = λ

D tX

> i=1

Φ( xi)M ∗αi, g 

E

> H

= λ

> t

X

> i=1

⟨Φ( xi)M ∗αi, g ⟩H

= λ

> t

X

> i=1

⟨M ∗αi, g (xi)⟩Y

= λ

> t

X

> i=1

⟨αi, M g (xi)⟩M,

> t

X

> i=1

⟨M g (xi), y i − M μ t(xi)⟩M =

> t

X

> i=1

D

M g (xi),

> t

X

> j=1

(M K (xi, x j )M ∗ + λI i=j )αj − M μ t(xi)

E

> M

= λ

> t

X

> i=1

D

M g (xi),

> t

X

> j=1

Ii=j αj

E

> M

= λ

> t

X

> i=1

⟨M g (xi), α i⟩M.

14 Hence, the minimum is attained at g = 0 .

## C Proof for Theorem 1 

This proof follows the same structure as [ 11 ], adapted to a general vector-valued setting. We highlight the necessary modifications when they arise. We first establish the result for the case M = I, where the learner observes the full output f (x). The general result in Theorem 1 then follows by applying the same argument to the composed function 

M f , which takes values in M and lies in a vector-valued RKHS with kernel KM . To start, we write 

KI = K for simplicity. We employ the feature representation of an operator-valued kernel [ 33 ], writing 

K(x, s ) = Φ ∗(x)Φ( s),

where Φ( x) ∈ L (Y, W) for some Hilbert space W. Then there exists w ∈ W such that 

f (·) = Φ ∗(·)w. 

Define the operator ΦXt ∈ L (W, Yt) by 

(Φ Xt w)i = Φ ∗(xi)w, ∀w ∈ W ,

and denote its adjoint by Φ∗ 

> Xt

. The vector-valued KRR estimator μt(x) can be expressed as 

μt(x) = Φ ∗(x)Φ ∗ 

> Xt

(Φ Xt Φ∗ 

> Xt

+ λI )−1Yt

= Φ ∗(x)Φ ∗ 

> Xt

(Φ Xt Φ∗ 

> Xt

+ λI )−1(Φ Xt w + δ)= Φ ∗(x)(Φ ∗ 

> Xt

ΦXt + λI )−1Φ∗ 

> Xt

(Φ Xt w + δ)= Φ ∗(x)(Φ ∗ 

> Xt

ΦXt + λI )−1(Φ ∗ 

> Xt

ΦXt w + Φ ∗ 

> Xt

δ)= f (x) − Φ∗(x)(Φ ∗ 

> Xt

ΦXt + λI )−1(λw − Φ∗ 

> Xt

δ),

where δ = ( δ1, · · · , δ t) ∈ Y t.Consequently, 

∥μt(x) − f (x)∥Y = Φ∗(x)(Φ ∗ 

> Xt

ΦXt + λI )−1(λw − Φ∗ 

> Xt

δ) Y

≤ ∥ (Φ ∗ 

> Xt

ΦXt + λI )−1/2Φ( x)∥op 

· ∥ (Φ ∗ 

> Xt

ΦXt + λI )−1/2(λw − Φ∗ 

> Xt

δ)∥W

≤ ∥ Φ∗(x)(Φ ∗ 

> Xt

ΦXt + λI )−1Φ( x)∥1/2op 

·



∥Φ∗ 

> Xt

δ∥(Φ ∗   

> XtΦXt+λI )−1

+ λ1/2∥w∥W



,

where we used ∥A∥op ≤ ∥ A∗A∥1/2op and the bound 

∥w∥(Φ ∗   

> XtΦXt+λI )−1

≤ λ−1/2∥w∥W .

Next, observe that 

λΦ∗(x)(Φ ∗ 

> Xt

ΦXt + λI )−1Φ( x) = Φ ∗(x)Φ( x) − Φ∗(x)Φ ∗ 

> Xt

(Φ Xt Φ∗ 

> Xt

+ λI )−1ΦXt Φ( x)= K(x, x ) − KxX t (KXtXt + λI )−1KXtx

= Kt(x, x ),

we have 

∥Φ∗(x)(Φ ∗ 

> Xt

ΦXt + λI )−1Φ( x)∥1/2op = λ−1/2∥Kt(x, x )∥1/2op .

For the noise term, applying Lemma 3 of [ 11 ] (with W = ℓ2) yields that, with probability at least 

1 − ζ, for all t ≥ 1,

∥Φ∗ 

> Xt

δ∥(Φ ∗   

> XtΦXt+λI )−1

≤ σ

q

2 log(1 /ζ ) + log det  I + λ−1Φ∗ 

> Xt

ΦXt

.

15 Using Sylvester’s determinant identity [21], 

det  I + λ−1Φ∗ 

> Xt

ΦXt

 = det  I + λ−1ΦXt Φ∗

> Xt

 = det  I + λ−1KXtXt

.

Combining the above bounds and observing that ∥w∥W = ∥f ∥H ≤ Γ, we obtain 

∥f (x) − μt(x)∥Y ≤



Γ + σ

√λ

q

2 log(1 /ζ ) + log det  I + λ−1KXtXt



∥Kt(x, x )∥1/2op .

Applying the same argument to the composed function M f , whose RKHS kernel is KM , yields Theorem 1. 

## D Proof for Theorem 2 

We first present a useful lemma from [11]. 

Lemma 3. For any λ > 0 and t ≥ 1, we have 

log det  I + λ−1KMXtXt

 =

> t

X

> i=1

log det  I + λ−1KMi−1(xi, x i).

From Lemma 3, we know that log det  I + λ−1KMXtXt

 is an increasing function with t. Hence, we conclude that βt ≤ βt+1 for all t ≥ 0.We can write the regret as 

RT =

> T

X

> t=1

F (x∗) − F (xt)=

> T

X

> t=1

F (x∗) − F t−1(xt) + F t−1(xt) − F (xt)=

> T

X

> t=1

F (x∗) − F t−1(x∗) + F t−1(x∗) − F t−1(xt) + F t−1(xt) − F (xt)

≤ 2

> T

X

> t=1

βt−1∥m∥M∥KMt−1(xt, x t)∥1/2op 

≤ 2βT +1 ∥m∥M

> T

X

> t=1

∥KMt−1(xt, x t)∥1/2op 

≤ 2βT +1 ∥m∥M

vuutT

> T

X

> t=1

∥KMt−1(xt, x t)∥op .

Substituting the definition of βT +1 yields the result. 

## E Proof for Theorem 3 

The following lemma shows that the operator-valued information quantities can be reduced to scalar information gain with separable kernels. This reduction is key to obtaining explicit regret bounds. 

Lemma 4. Let K = G ⊗ B. The operator-valued quantities in Theorem 2 are upper bounded by the scalar information gain associated with G. In particular, 

log det  I + λ−1GXT XT ⊗ BM

 ≤

> ∞

X

> i=1

γT G, λλBM

> i

!

,

> T

X

> t=1

∥KMt−1(xt, x t)∥op ≤ 2CK γT (G, λCK

),

16 where {λBM 

> i

}i denotes the point spectrum of BM , and γT (G, λ ) = max XT log det  I +λ−1GXT XT



is the information gain associated with the scalar kernel G.

The proof of Lemma 4 is provided in Appendix F. For many commonly used scalar kernels G, the information gain γT (G, λ ) admits sublinear growth in T . However, one can observe that log det  I +

λ−1GXT XT ⊗ BM

 is upper bounded by an infinite sum of information gains with increasing noise variance, which is not guaranteed to be finite. To guarantee a sublinear regret bound, we further assume that the spectrum decomposition of BM is composed of a finite series, i.e., BM = Pni=1 λBM 

> i

ϕiϕ∗ 

> i

.Applying the result from Lemma 4 in Theorem 2, we can bound the cumulative regret by 

RT ≤ 2∥m∥M

Γ + σ

√λ

vuut2 log( 1

ζ ) + 

> n

X

> i=1

γT G, λλBM

> i

!r

2CK T γ T (G, λCK

).

For the linear kernel, 

γT = O(log T ),

for the square exponential kernel, 

γT = O((log T )d+1 ),

for the Matérn kernel with ν > 1,

γT = O(T d(d+1) /(2 ν+d(d+1)) log T ).

Consequently, we have 1. Linear kernel: RT ≤ O  log T √T ,2. Gaussian kernel: RT ≤ O  (log T )d+1 √T ),3. Matérn kernel: RT ≤ O  T d(d+1) /(2 ν+d(d+1)) log T √T .

## F Proof for Lemma 4 

For the first part, according to the Fredholm determinant for positive definite trace class operators [35], we observe that 

log det  I + λ−1GXT XT ⊗ BM

 =

> T

X

> t=1
> ∞

X

> i=1

log(1 + 1

λ λGt λBM 

> i

) ≤ 1

λ

> T

X

> t=1
> ∞

X

> i=1

λGt λBM 

> i

< ∞.

Hence, applying Fubini’s theorem for infinite series (switching the order of summation for an absolutely convergent series), we obtain 

log det  I + λ−1GXT XT ⊗ BM

 =

> T

X

> t=1
> ∞

X

> i=1

log(1 + 1

λ λGt λBM 

> i

)=

> ∞

X

> i=1
> T

X

> t=1

log(1 + 1

λ λGt λBM 

> i

)=

> ∞

X

> i=1

log det  I + λBM

> i

λ GXT XT )

≤

> ∞

X

> i=1

γT (G, λλBM

> i

).

For the second part, we first present a useful lemma. 

Lemma 5. The operator norm of KMt−1(xt, x t) is given by 

∥KMt−1(xt, x t)∥op = max  

> j

λBM

> j



G(xt, x t) − GxtXt−1



GXt−1Xt−1 + λλBM

> j

−1

GXt−1xt



,

where λBM 

> j

is the point spectrum of BM .

17 Proof. Given that BM is a trace-class operator, we denote its spectrum decomposition by BM =

P∞

j=1 λBM

j ϕj ϕ∗

j . Similarly, GXt−1Xt−1 = Pt−1

i=1 λGi θiθ∗

i . Consequently, we have 

(GXt−1Xt−1 ⊗ BM + λI )−1 =

t−1X

i=1 

∞X

j=1 

(λGi λBM

j +λ)−1(θi ⊗ ϕj )( θi ⊗ ϕj )∗

=

t−1X

i=1 

∞X

j=1 

(λGi λBM

j +λ)−1(θi ⊗ ϕj )( θ∗

i ⊗ ϕ∗

j )=

∞X

j=1 

t−1X

i=1 

(λGi λBM

j +λ)−1(θiθ∗

i ) ⊗ (ϕj ϕ∗

j )=

∞X

j=1 



λBM

j GXt−1Xt−1 + λI 

−1

⊗ (ϕj ϕ∗

j ).

For KMt−1(xt, x t), we have 

KMt−1(xt, x t)= G(xt, x t) ⊗ BM − (GxtXt−1 ⊗ BM )( GXt−1Xt−1 ⊗ BM + λI )−1(GXt−1xt ⊗ BM )=

∞X

j=1 

G(xt, x t)λBM

j ϕj ϕ∗

j −

∞X

j=1 

GxtXt−1



λBM

j GXt−1Xt−1 + λI 

−1

GXt−1xt (BM ϕj ϕ∗

j BM )=

∞X

j=1 



G(xt, x t)λBM

j − (λBM

j )2Gxt−1Xt−1



λBM

j GXt−1Xt−1 + λI 

−1

GXt−1xt



ϕj ϕ∗

j .

Following this expression, we conclude the proof by choosing the largest point spectrum, which is a well-defined value given that KMt−1(x, x ) is a bounded positive trace-class operator. Now, according to Mercer’s theorem [ 34 ], there exist a mapping φ : X → ℓ2 such that 

G(x, x ) = φ⊤(x)φ(x). We also define an operator φXt−1 : ℓ2 → Rt−1 via φXt−1 a =[φ(x1)⊤a, . . . , φ (xt−1)⊤a]⊤, ∀a ∈ ℓ2. Then GXt−1Xt−1 can be expressed as φXt−1 φ⊤

Xt−1 . Then, applying the Woodbury identity we have 

G(x, x ) − GxX t−1 (GXt−1Xt−1 + λI )−1GXt−1x

= φ(x)⊤φ(x) − φ(x)⊤φ⊤

Xt−1 (φXt−1 φ⊤

Xt−1 + λI )−1φXt−1 φ(x)= λφ (x)⊤(φ⊤

Xt−1 φXt−1 + λI )−1φ(x).

Applying Lemma 5, we have 

∥KMt−1(xt, x t)∥op = max 

j λBM

j



G(xt, x t) − GxtXt−1



GXt−1Xt−1 + λλBM

j

I

−1

GXt−1xt



= max 

j λφ (xt)⊤(φ⊤

Xt−1 φXt−1 + λλBM

j

I)−1φ(xt)

≤ λφ (xt)⊤(φ⊤

Xt−1 φXt−1 + λCK

I)−1φ(xt)= CK



G(xt, x t) − GxtXt−1 (GXt−1Xt−1 + λCK

I)−1GXt−1xt



.

Summing over t, we have 

TX

t=1 

∥KMt−1(xt, x t)∥op ≤ CKTX

t=1 

G(xt, x t) − GxtXt−1 (GXt−1Xt−1 + λCK

I)−1GXt−1xt

≤ CK log det( I + CK

λ GXT XT )

≤ 2CK γT (G, λCK

).

18 Algorithm 2 Time-varying vector-valued Bayesian optimization 

Require: Total budget T , regularization parameter λ > 0, confidence level ζ ∈ (0 , 1)  

> 1:

Initialize dataset X0 = ∅, Y0 = ∅ with M μ 0(x) = 0 , KM 

> 0

(x, x ) = KM (x, x ) 

> 2:

for t = 1 , . . . , T do  

> 3:

Compute M μ t−1(x) and KMt−1(x, x ) 

> 4:

Select 

xt ∈ arg max  

> x∈X

⟨mt, M μ t−1(x)⟩M + βt−1∥mt∥M∥KMt−1(x, x )∥1/2op  

> 5:

Query f at xt and observe yt = M f (xt) + δt 

> 6:

Update Xt, Y t 

> 7:

end for 

## G Extension to time-varying and nonlinear objectives 

G.1 Time-varying objective 

When the functional mt varies over time, the objective becomes Ft(x) = ⟨mt, M f (x)⟩M. At each iteration, the objective admits uncertainty bounds Ft(x) ∈ [F t(x), F t(x)] , where 

F t(x) = ⟨mt, M μ t−1(x)⟩M + ∥mt∥M ∥M f (x) − M μ t−1(x)∥M,F t(x) = ⟨mt, M μ t−1(x)⟩M − ∥ mt∥M ∥M f (x) − M μ t−1(x)∥M.

Since only the functional mt varies while the estimator of M f remains unchanged, the uncertainty quantification in the measurement space can be reused across time. This allows the framework to handle time-varying objectives without modifying the underlying estimator. Based on this formulation, we present Algorithm 2 for sequential optimization under time-varying objectives. The algorithm follows the same principle as its static counterpart, with the key difference that the confidence width βt−1∥mt∥M ∥KMt−1(x, x )∥1/2op now depends on the current objective mt.This adaptation allows the uncertainty quantification to adjust dynamically to changes in the objective, leading to more accurate exploration–exploitation trade-offs. 

G.2 Nonlinear Lipschitz objective 

Let F ′ : M → R be LF ′ -Lipschitz, i.e., |F ′(m1) − F ′(m2)| ≤ LF ′ ∥m1 − m2∥M. Applying this with m1 = M f (x) and m2 = M μ t(x) gives 

F ′(M f (x)) ≤ F ′(M μ t(x)) + LF ′ ∥M f (x) − M μ t(x)∥M,F ′(M f (x)) ≥ F ′(M μ t(x)) − LF ′ ∥M f (x) − M μ t(x)∥M.

Thus, the same uncertainty bound in the measurement space can be propagated to nonlinear objectives via Lipschitz continuity. The remainder of the argument follows from the linear case. 

## H Efficient computation 

Consider a separable kernel of the form KM (x, s ) = G(x, s ) BM , where 

BM =

> ∞

X

> i=1

λBM 

> i

ϕiϕ∗

> i

is a positive trace-class operator. Although BM is trace-class, it can have infinitely many nonzero eigenvalues. 

Finite-rank approximation. To obtain a tractable representation, we approximate BM by truncat-ing its spectral decomposition to the leading n components: 

BM,n =

> n

X

> i=1

λBM 

> i

ϕiϕ∗ 

> i

.

19 This yields a finite-rank approximation that captures the dominant spectral components and enables efficient computation. Under this approximation, any observation yi can be represented in the basis {ϕi}ni=1 as 

yi =

> n

X

> j=1

¯Yij ϕj ,

where ¯Yt ∈ Rt×n collects the corresponding coefficients. 

Representation of the estimator. Using this basis representation, the coefficients of M μ t(x) can be computed explicitly. In particular, the coefficient corresponding to ϕi is given by 

λBM,n  

> i

GxX t

 λBM,n  

> i

GXtXt + λI −1 ¯Y:,i ,

where ¯Y:,i ∈ Rt denotes the i-th column of ¯Yt.

Efficient evaluation. The above representation allows efficient computation of key quantities. For example, 

log det  λ−1GXtXt ⊗BM,n +I

=

> n

X

> j=1

log det  λBM,n  

> j

λ−1GXtXt + I,

⟨m, M μ t(x)⟩M =

> n

X

> i=1

¯miλBM,n  

> i

GxX t

 λBM,n  

> i

GXtXt + λI −1 ¯Y:,i ,KMt (x, x ) = 

> n

X

> j=1



G(x, x )λBM,n  

> j

−(λBM,n  

> j

)2GxX t

 λBM,n  

> j

GXtXt +λI −1GXtx



ϕj ϕ∗ 

> j

,

where ¯mi is the coordinate of m under basis {ϕi}ni=1 , i.e., m = Pni=1 ¯miϕi.

Optimization. To select the next query point, we solve 

xt ∈ arg max  

> x∈X

⟨m, M μ t−1(x)⟩M + βt−1∥m∥M∥KMt−1(x, x )∥1/2op .

This objective is generally non-convex in x. For low-dimensional problems, one can discretize the input space and perform grid search. For higher-dimensional settings, gradient-based optimization with multiple random initializations can be used. 

## I Numerical experiments 

I.1 Experiment setup Objective design. To enable a fair comparison across methods with different observation models, we construct the objective independently of the measurement operator. Let f (x) ∈ Y denote the under-lying vector-valued output. We define the objective as F (x) = ⟨m, f (x)⟩Y , m = Pqj=1 wj ξj , where 

Ξ = [ ξ1, . . . , ξ q ] : Rq → Y is a collection of linear functionals represented in Y, and w ∈ Rq are the associated weights. We compactly write m = Ξ w. This formulation is convenient for evaluating vvBO under multiple observation models. In the experiments, we consider: (i) full observation, where 

M = I, M = Y, and the full function f (x) is observed; (ii) finite-dimensional observation, where 

M = Ξ ∗, M = Rq , and only the projections Ξ∗f (x) = [ ⟨ξ1, f (x)⟩Y , . . . , ⟨ξq , f (x)⟩Y ]⊤ ∈ Rq are available. Under the finite-dimensional observation model, F (x) = ⟨m, f (x)⟩Y = ⟨w, Ξ∗f (x)⟩Rq ,which provides a unified formulation for benchmark methods with different measurement assump-tions. In particular, methods operating on scalar, finite-dimensional, or function-valued measurements can all be compared while optimizing an equivalent objective derived from the same black-box operator f .20 Implementation of baseline methods. Here are the implementation details for each baseline method. • Scalar-valued BO (BO). For vanilla Bayesian optimization, only the scalar objective value 

F (x) is measured at each query. • Retrained scalar-valued BO (rBO). A variant of BO that is re-initialized whenever the objective changes, discarding previously collected data. • Multi-task BO (MTBO). Multi-task BO assumes access to vector-valued observations in a finite-dimensional space [11]. In our experiments, for each x ∈ X , MTBO measures 

Ξ∗f (x) ∈ Rq ,

and constructs a surrogate model mapping from x ∈ X to Ξ∗f (x) ∈ Rq . The objective is then evaluated as 

F (x) = ⟨w, Ξ∗f (x)⟩Rq .

For vvBO, when M = Ξ ∗, vvBO and MTBO operate on the same finite-dimensional observations. In contrast, under full observations ( M = I), vvBO directly models the full output f (x), while MTBO remains restricted to the projected measurements Ξ∗f (x) ∈ Rq .• Retrained multi-task BO (rMTBO). A variant of MTBO that is re-initialized whenever the objective changes, discarding previously collected data. Since the objective changes through the weights w, rMTBO updates the scalarized objective accordingly while reconstructing the model from scratch using the newly collected observations. • Contextual BO (CTBO). The objective F (x) = ⟨m, f (x)⟩M can equivalently be written as a scalar-valued function ˜F : X × Y → R defined by 

˜F (x, m ) := ⟨m, f (x)⟩Y .

Thus, F (x) = ˜F (x, m ) where m acts as a context variable when the objective changes. If f belongs to a vector-valued RKHS with kernel K, then ˜F lies in a scalar-valued RKHS with kernel [7, 33] 

˜K (x1, m 1), (x2, m 2) = ⟨m1, K (x1, x 2)m2⟩Y .

Given observations 

˜Xt = {(xi, m i)}ti=1 , ˜Yt = { ˜F (xi, m i)}ti=1 ,

the standard scalar-valued GP regression can be applied with posterior mean and variance given by 

˜μt(x, m ) = ˜K(x,m ) ˜Xt

  ˜K ˜Xt ˜Xt + λI −1 ˜Yt,

˜σ2 

> t

(x, m ) = ˜K(( x, m ), (x, m )) − ˜K(x,m ) ˜Xt

  ˜K ˜Xt ˜Xt + λI −1 ˜K ˜Xt(x,m ).

Common acquisition functions such as UCB, expected improvement, and Thompson sam-pling can then be applied [16]. In this work, we use the UCB acquisition function. • Function-on-function BO (FFBO). FFBO is implemented following [ 20 ], corresponding to CTBO with a fixed context. The observations are scalar objective F (x).

RKHS representation of vector-valued outputs. In our experiments, the system output f (x) is a function or a trajectory. For numerical implementation, we construct a finite-dimensional representa-tion. Specifically, for each input x, the function f (x) is evaluated on a dense grid {tj }50 

> j=1

, yielding samples yj (x) = f (x)( tj ). We then approximate f (x) using df (x), which is in a scalar-valued RKHS and expressed as df (x)( t) = P50  

> j=1

αf (x),j k(t, t j ), where k is a scalar-valued kernel defined on the output domain. The coefficient αf (x) = [ αf (x),1, . . . , α f (x),50 ]⊤ ∈ R50 are coefficients obtained by solving a scalar-valued KRR problem 

min  

> df(x)∈H k
> 50

X

> j=1

  df (x)( tj ) − yj (x)2 + 0 .01 ∥ df (x)∥2 

> Hk

.

21 Table 1: Hyperparameters for different test operators. Test Operator Kernel λ Noise level Input length scale(s) Output length scale(s) GP RBF 10 −2 10 −2 0.1 0.1

GP (3D) RBF 10 −2 10 −2 0.1 0.1

Ackley RBF 10 −2 10 −2 3 3

Eggholder RBF 10 −2 10 0 50 50 

Bukin RBF 10 −2 10 0 0.6 1

Holder Table RBF 10 −2 10 0 1 1

Shubert RBF 10 −2 10 −3 0.5 0.5

Langermann RBF 10 −2 10 −3 0.5 0.5

Controller tuning RBF 10 −2 10 −1 [0 .6, 0.8, 0.8] [5000 , 10000] 

Table 2: Computation time (ms) of vvBO across different iterations (mean ± std) for different test operators. Test Operator Iteration 30 Iteration 60 Iteration 100 GP 8.16 ± 0.04 8.59 ± 0.10 9.22 ± 0.17 

Ackley 10 .08 ± 0.22 10 .62 ± 0.23 11 .42 ± 0.19 

Eggholder 8.13 ± 0.19 8.53 ± 0.18 9.20 ± 0.20 

Bukin 8.24 ± 0.15 8.56 ± 0.18 9.39 ± 0.18 

Holder Table 10 .13 ± 0.25 10 .60 ± 0.18 11 .39 ± 0.27 

Shubert 7.98 ± 0.17 8.39 ± 0.15 9.22 ± 0.26 

Langermann 8.06 ± 0.16 8.42 ± 0.09 9.11 ± 0.19 

Similarly, each linear functional ξ is represented in the same basis as ξ(·) = P50  

> j=1

αξ,j k(·, t j ), with coefficients αξ,j ∈ R. Linear functionals of f (x) are then evaluated as RKHS inner products in this finite-dimensional representation. All experiments use this finite representation of f (x) and the functionals ξ, ensuring a consistent and fair comparison. This construction is introduced solely for numerical implementation and does not alter the theoretical results derived in this work. 

Hyperparameters. All simulations are conducted on HP OMEN 45L Desktop (Intel Core Ultra 9 285K, 24 cores up to 6.5 GHz), 62 GB RAM, running Ubuntu 24.04.3 LTS (Linux 6.14). The hyperparameters for different test operators are summarized in Table 1 (identical across different baseline methods). The kernel used for vvBO is composed of an RBF kernel and the identity operator. The computation times for those test operators at different iterations for vvBO are shown in Table 2. 

I.2 Synthetic problem I.2.1 Test operators 

We evaluate the proposed method on synthetic problems constructed from standard optimization benchmarks. For each benchmark, the input is decomposed as u = ( x, t ), where x denotes the input variable and t indexes the structured output. The latent vector-valued function is generated as 

f (x)( t) = h(x, t ),

where h is one of the following ground-truth functions. The scalar objective is then obtained by applying a linear functional to the trajectory f (x).

Gaussian process. For the GP benchmark, we generate a smooth function using an RBF kernel expansion. Let XNx = {xi}Nx 

> i=1

and TNt = {tj }10  

> j=1

denote fixed grids on [0 , 1] . Nx changes with grid density. For one-dimensional inputs, Nx = 10 . For three-dimensional inputs, Nx = 125 . Let 

α ∈ RNx×Nt be a matrix with randomly sampled elements, i.e., αij is sampled uniformly from 

[−3.5, 3.5] . The ground truth is h(x, t ) = GxX Nx αG TNt t, where G is an RBF kernel with length scale ℓ = 0 .1. We implement both a one-dimensional version, denoted GP, and a higher-dimensional version, denoted GP (3D), where x ∈ R3.22 Ackley. For u ∈ Rd, the Ackley benchmark is 

h(u) = −

−20 exp 

−0.2

vuut 1

d

> d

X

> i=1

u2

> i

 − exp 1

d

> d

X

> i=1

cos(0 .2πu i)

!

+ 20 + e − 20 

 .

The domain is ui ∈ [−32 .768 , 32 .768] .

Eggholder. For u = ( u1, u 2),

h(u) = −

 u2

2 + 47 



sin 

r 12

u2

2 + u1

4 + 47 

!

− u1

2 sin 

r 12

u1

2 −

 u2

2 + 47 

 !

,

with domain ui ∈ [−512 , 512] .

Bukin. For u = ( u1, u 2),

h(u) = −100 

q

|u2 − 0.01 u21| + 0 .01 |u1 + 10 | + 180 ,

with u1 ∈ [−15 , −5] and u2 ∈ [−3, 3] .

Holder Table. For u = ( u1, u 2),

h(u) = sin( u1) cos( u2) exp 1 −

pu21 + u22

π

!

,

with ui ∈ [−10 , 10] .

Shubert. For u = ( u1, u 2),

h(u) = 1100 

" 5X

> i=1

i cos 



(i + 1) u1

2 + i

# " 5X

> i=1

i cos 



(i + 1) u2

2 + i

#

,

with ui ∈ [−10 , 10] .

Langermann. For u = ( u1, u 2),

h(u) = 

> 5

X

> i=1

ci exp 



− ∥u/ 2 − Ai∥22

π



cos  π∥u/ 2 − Ai∥22

 ,

where 

c = (1 , 2, 5, 2, 3) , A =



3 55 22 11 47 9

 ,

with ui ∈ [0 , 10] .

I.2.2 Implementation details 

We consider three phases with changing objectives. Each objective is defined as Fi(x) = ⟨mi, f (x)⟩Y ,

mi = Ξ iwi, where Ξi = [ ξi, 1, . . . , ξ i, 5] : R5 → Y collects a set of base functionals and wi ∈ R5 are the associated weights. From phase one to two, only the weights wi change while the functional basis 

Ξi remains fixed. From phase two to three, both Ξi and wi are modified. For vvBO, we consider two observation regimes. Under full observations ( M = I), the full output f (x) is observed and all three phases are evaluated. Under partial observations ( M = Ξ ∗), only projected measurements 

Ξ∗f (x) ∈ R5 are available. In this case, Fi(x) = ⟨mi, f (x)⟩Y = ⟨wi, Ξ∗ 

> i

f (x)⟩R5 . Since the measurement operator is fixed to Ξ∗, only the transition from phase one to two is considered under 23 Table 3: Functional basis Ξi and weights wi defining the objectives Fi(x) = ⟨Ξiwi, f (x)⟩Y across phases. From phase one to two, only the weights wi change, while from phase two to three both Ξi

and wi are modified. Test Operator Phase Functional basis Ξi wi ∈ R5 β

GP, GP(3D) one [ξ(0) , ξ (0 .1) , ξ (0 .2) , ξ (0 .3) , ξ (0 .4)] 15 [1 , 1, 1, 1, 1] 6two same as phase one [0 , 1, 0, 0, 0] 6three [ξ(0 .5) , ξ (0 .6) , ξ (0 .7) , ξ (0 .8) , ξ (0 .9)] 15 [1 , 1, 1, 1, 1] 6Ackley one [ξ(1) int , . . . , ξ (5) int ] 15 [1 , 1, 1, 1, 1] 10 two same as phase one 14 [1 , 0, 1, 1, 1] 40 three [ξ(1) int , . . . , ξ (5) int ] 15 [1 , 1, 1, 1, 1] 70 Bukin one [ξ(0) , ξ (0 .5) , ξ (1) , ξ (1 .5) , ξ (2)] 15 [1 , 1, 1, 1, 1] 100 two same as phase one [0 , 0, 0, 0, 1] 115 three [ξ(0) , ξ (−0.5) , ξ (−1) , ξ (−1.5) , ξ (−2)] 15 [1 , 1, 1, 1, 1] 80 Eggholder one [ξ(500) , ξ (400) , ξ (300) , ξ (200) , ξ (100)] [1 , 0, 0, 0, 0] 400 two same as phase one [0 , 0, 1, 0, 0] 250 three [ξ(0) , ξ (−100) , ξ (−200) , ξ (−300) , ξ (−400)] [0 , 0, 0, 0, 1] 300 Holder Table one [ξ(1) int , . . . , ξ (5) int ] 15 [1 , 1, 1, 1, 1] 30 two same as phase one [1 , 0, 0, 0, 0] 30 three [ξ(1) int , . . . , ξ (5) int ] 15 [1 , 1, 1, 1, 1] 5Shubert one [ξ(0) , ξ (1) , ξ (2) , ξ (3) , ξ (4)] 15 [1 , 1, 1, 1, 1] 0.5 two same as phase one [0 , 0, 0, 1, 0] 0.5 three [ξ(0) , ξ (−1) , ξ (−2) , ξ (−3) , ξ (−4)] [0 , 0, 0, 0, 1] 1Langermann one [ξ(5) , ξ (6) , ξ (7) , ξ (8) , ξ (9)] 15 [1 , 1, 1, 1, 1] 3two same as phase one [1 , 0, 0, 0, 0] 3three [ξ(0) , ξ (1) , ξ (2) , ξ (3) , ξ (4)] [1 , 0, 0, 0, 0] 3partial observations, while phase three is omitted because changing Ξi would also modify the measurement operator. The corresponding functional bases Ξi and weights wi for each test operator are summarized in Table 3. Here, ξint denotes a randomly generated integral functional, while ξ(t) denotes a point-evaluation functional at t. Table 4 summarizes the inputs and measurements available to each method across phases. In particular, vvBO with full observations ( M = I) directly models the full output 

f (x), whereas vvBO with partial observations ( M = Ξ ∗) only accesses the projected measurements 

Ξ∗f (x) ∈ R5. MTBO and rMTBO operate on finite-dimensional observations constructed from 

Ξ∗ 

> i

f (x) ∈ R5, while BO and rBO only observe the scalar objective values. CTBO treats mi as a context variable and models the scalar-valued mapping (x, m i) 7 → ˜F (x, m i). FFBO corresponds to CTBO with fixed context and only observes scalar objective values. 

I.2.3 Additional experiments Full observation. We report additional experiments under full observation ( M = I), where the complete output f (x) is available. The results are shown in Fig. 4. These results confirm that vvBO adjusts effectively across varying objectives and that the benefit of structured measurements persists across different benchmarks. 

Partial observation. We further evaluate the proposed method under partial observation settings, where the measurement operator maps the output to a finite-dimensional space ( M = Ξ ∗). The results are shown in Fig. 5 and Fig. 6. Across all test operators, vvBO consistently outperforms the baseline methods in terms of both simple regret and cumulative regret, effectively accommodating to different objectives. In contrast, scalar-valued BO methods suffer from information loss due to observing only the objective value, while MTBO has limited performance and does not adapt to 24 Table 4: Inputs, measurements, and objective evaluation for different baseline methods in the synthetic experiments. 

Method Phase Input Measurement Surrogate objective vvBO ( M = I)one x f (x) ⟨Ξ1w1, f (x)⟩Y

two x f (x) ⟨Ξ2w2, f (x)⟩Y

three x f (x) ⟨Ξ3w3, f (x)⟩Y

vvBO ( M = Ξ ∗)one x Ξ∗ 

> 1

f (x) ⟨w1, Ξ∗ 

> 1

f (x)⟩R5

two x Ξ∗ 

> 2

f (x) ⟨w2, Ξ∗ 

> 2

f (x)⟩R5

three – – –MTBO one x Ξ∗ 

> 1

f (x) ⟨w1, Ξ∗ 

> 1

f (x)⟩R5

two x Ξ∗ 

> 1

f (x) ⟨w1, Ξ∗ 

> 1

f (x)⟩R5

three x Ξ∗ 

> 1

f (x) ⟨w1, Ξ∗ 

> 1

f (x)⟩R5

rMTBO one x Ξ∗ 

> 1

f (x) ⟨w1, Ξ∗ 

> 1

f (x)⟩R5

two x Ξ∗ 

> 2

f (x) (re-initialized) ⟨w2, Ξ∗ 

> 2

f (x)⟩R5

three x Ξ∗ 

> 3

f (x) (re-initialized) ⟨w3, Ξ∗ 

> 3

f (x)⟩R5

BO one x F1(x) F1(x)

two x F2(x) F2(x)

three x F3(x) F3(x)

rBO one x F1(x) F1(x)

two x F2(x) (re-initialized) F2(x)

three x F3(x) (re-initialized) F3(x)

CTBO one (x, Ξ1w1) F1(x) ˜F (x, Ξ1w1)

two (x, Ξ2w2) F2(x) ˜F (x, Ξ2w2)

three (x, Ξ3w3) F3(x) ˜F (x, Ξ3w3)

FFBO one (x, Ξ1w1) F1(x) ˜F (x, Ξ1w1)

two (x, Ξ1w1) F2(x) ˜F (x, Ξ1w1)

three (x, Ξ1w1) F3(x) ˜F (x, Ξ1w1)

changes in the objective function. These results demonstrate that vvBO retains its advantage under different observation models. 

Comparison between vvBO and CTBO. The performance of CTBO in tracking changing objec-tives depends critically on the variation of the context m. When m shifts to a distant region, CTBO lacks sufficient samples in that region to construct an accurate model, resulting in more exploration. In contrast, when m changes only slightly, CTBO can effectively reuse existing samples from the previous context to reduce uncertainty. This behavior is illustrated in Fig. 7. From phase one to phase two, we first change the weight from [1 , 0, 0, 0, 0] to [0 , 0, 1, 0, 0] , corresponding to optimizing only the third component ξ3. This induces a large shift in the context, leading to a significant increase in cumulative regret due to the need for extensive exploration in a previously unseen region. In contrast, when the weight is changed from [1 , 0, 0, 0, 0] to [0 .8, 0.2, 0, 0, 0] , the induced change is small. In this case, CTBO can leverage previously collected data more effectively, resulting in reduced exploration cost and lower cumulative regret. The corresponding uncertainty estimates for CTBO are shown in Fig. 8. When transitioning to [0 , 0, 1, 0, 0] , the model exhibits high uncertainty due to the lack of nearby samples. For the smaller change, the uncertainty remains well-controlled, enabling more efficient exploration. These results highlight the key limitation of CTBO. The performance is highly sensitive to the variation of the context. In contrast, vvBO models the underlying vector-valued oper-ator directly and therefore maintains consistent performance regardless of changes in the objective functional. 

I.3 Real-world controller tuning 

We evaluate the proposed method on a real-world building control task using the Building Optimiza-tion Testing Framework (BOPTEST) [ 2], a high-fidelity simulation framework for benchmarking advanced HVAC control strategies. BOPTEST provides physics-based building emulators imple-25 0 50 100 150 200 250 300                                                          

> 0.0
> 2.5
> 5.0
> 7.5 Phase one Phase two Phase three
> GP (3D)
> Simple regret
> 050 100 150 200 250 300
> 0
> 500
> 1000
> Phase one Phase two Phase three
> Cumulative regret
> 020 40 60 80 100
> 0
> 50
> 100
> Bukin
> 020 40 60 80 100
> 0
> 1000
> 2000
> 3000
> 020 40 60 80 100
> 0
> 20
> Holder Table
> 020 40 60 80 100
> 0
> 500
> 1000
> 020 40 60 80 100
> 0.0
> 0.5
> 1.0
> 1.5
> Shubert
> 020 40 60 80 100
> 0
> 20
> 40
> 020 40 60 80 100
> Iteration
> 0
> 2
> 4
> 6
> Langermann
> 020 40 60 80 100
> Iteration
> 0
> 50
> 100
> 150
> vvBO (ours) CTBO MTBO [11] rMTBO [11] BO rBO FFBO [20]

Figure 4: Comparison of simple regret and cumulative regret for additional test operators under full observations ( M = I). mented in Modelica and exposed through standardized APIs, enabling realistic closed-loop evaluation under dynamic weather, occupancy, and pricing conditions [2]. We consider the ‘ singlezone_commercial_hydronic ’ test case, which models a commercial building zone served by a hydronic radiator connected to a district heating network. The building dynamics include thermal interactions between the indoor air, building envelope, external weather conditions, solar gains, and internal heat loads. Heating is delivered through a water-based radiator system, resulting in slow thermal dynamics and delayed control responses that make long-horizon optimization challenging. The controller regulates the radiator valve opening u1 ∈ [0 , 1] , which determines the heat supplied to the zone through the district heating loop. Further details on this test case can be found in [47]. 26 0                    

> 2
> 4
> Phase one Phase two
> GP
> Simple regret
> 0
> 25
> 50
> 75
> Phase one Phase two
> Cumulative regret
> 0
> 20
> 40
> 60
> Ackley
> 0
> 500
> 1000
> 010 20 30 40 50 60
> Iteration
> 0
> 200
> 400
> 600
> Eggholder
> 010 20 30 40 50 60
> Iteration
> 0
> 5000
> 10000
> 15000
> vvBO (ours) CTBO MTBO [11] rMTBO [11] BO rBO FFBO [20]

Figure 5: Comparison of simple regret and cumulative regret for three test operators under partial observations ( M = Ξ ∗). 

Controller design. We employ a model predictive control (MPC) scheme to regulate the indoor temperature. The system dynamics are modeled using an AutoRegressive with eXogenous input (ARX) model: 

yk+1 = a⊤yk−10: k +

> 3

X

> i=1

b⊤ 

> i

ui,k −10: k,

where y is the indoor temperature, and u1, u 2, u 3 correspond to the valve position, outdoor temper-ature, and solar irradiation. In this formulation, only the valve position is controlled. The outdoor temperature and solar irradiation are read directly from the BOPTEST API. The system model coefficients are obtained by solving a least squares problem with the data collected from a randomized bang-bang controller, i.e., the radiator is turned on when the temperature falls below a lower bound, and turned off when it exceeds an upper bound, and randomly actuated otherwise. The optimization problem for the MPC controller is then formulated as 

min  

> u1,ϵ
> N−1

X

> k=0

 u1,k + θ2ϵk + θ3(u1,k − u1,k −1)2

s.t. yk+1 = a⊤yk−10: k +

> 3

X

> i=1

b⊤ 

> i

ui,k −10: k,θ1 − ϵk ≤ yk+1 ≤ 26 + ϵk, k = 0 , . . . , N − 1,u1,k ∈ [0 , 1] , ϵk ≥ 0, k = 0 , . . . , N − 1.

where N = 64 is the horizon, ϵk is the slack variable to ensure feasibility. The MPC controller is parameterized by θ = ( θ1, θ 2, θ 3), corresponding to the comfort bound, slack penalty, and smoothness penalty. The controller operates with a sampling time of 900 s, meaning that the control input is updated every 15 minutes. We evaluate the controller on a daily basis. For a fixed θ, the controller regulates the indoor temperature over a 24-hour window, producing the heating trajectory Q(t) and the indoor temperature trajectory y(t), where t denotes the time of day in seconds. 27 0 50 100 150 200                                                               

> 0.0
> 2.5
> 5.0
> 7.5 Phase one Phase two
> GP (3D)
> Simple regret
> 050 100 150 200
> 0
> 250
> 500
> 750 Phase one Phase two
> Cumulative regret
> 010 20 30 40 50 60
> 0
> 50
> 100
> Bukin
> 010 20 30 40 50 60
> 0
> 1000
> 2000
> 010 20 30 40 50 60
> 0
> 20
> Holder Table
> 010 20 30 40 50 60
> 0
> 500
> 1000
> 010 20 30 40 50 60
> 0.0
> 0.2
> Shubert
> 010 20 30 40 50 60
> 0
> 5
> 10
> 010 20 30 40 50 60
> Iteration
> 0
> 1
> 2
> 3
> Langermann
> 010 20 30 40 50 60
> Iteration
> 0
> 20
> 40
> vvBO (ours) CTBO MTBO [11] rMTBO [11] BO rBO FFBO [20]

Figure 6: Comparison of simple regret and cumulative regret for additional test operators under partial observations ( M = Ξ ∗). 

Objective. The goal is to optimize the controller parameters by minimizing the daily cost 

Jτ (θ) = 

Z

Q(t)pheating ,τ (t) dt + 0 .1

Z

Q(t)pCO 2 (t) dt + 100 

 1

|T | 

Z

y(t) dt − 24 

2

,

where τ denotes the day index. These three components, which are nonlinear black-box functions with respect to θ, capture energy cost, environmental impact, and thermal discomfort, respectively. Let f (θ) = ( Q(t), y (t)) represent the vector-valued system output. The first two terms correspond to linear functionals of f (θ), while the third term is a nonlinear functional of the temperature trajectory. The heating price pheating ,τ (t) varies across days following a sinusoidal pattern between 0 and 0.232 

EUR/kWh, making the objective Jτ (θ) inherently time-varying. 28 0                

> 200
> 400
> 600 Phase one Phase two
> Large context change  Simple regret
> 0
> 5000
> 10000
> Phase one Phase two
> Cumulative regret
> 010 20 30 40 50 60
> Iteration
> 0
> 200
> 400
> 600
> Small context change
> 010 20 30 40 50 60
> Iteration
> 0
> 5000
> 10000
> vvBO (ours) CTBO

Figure 7: Comparison between vvBO and CTBO for the eggholder test operator for different contexts. When the change in the context is small, CTBO has a lower exploration cost. 500          

> 0
> 500
> Large context change  End of phase one Start of phase two
> 512 256 0256 512
> t
> 500
> 0
> 500
> Small context change
> 512 256 0256 512
> t

Figure 8: Comparison of the confidence bounds of CTBO for the eggholder test operator for different objectives. When the change in the context is small, CTBO can formulate a more informative confidence bound. 

Learning setup. The mapping from θ to trajectories Q(t) and y(t) is generally unknown. In vvBO, we model such mapping using a vector-valued RKHS model with separable kernel, i.e., K = G ⊗ B.Specifically, we choose the scalar kernel G as a radial basis function kernel and set B to be the identity operator on Y. We assume the full trajectories f (θ) are available for measurement. For numerical implementation, trajectories are discretized over time and represented using the scalar-valued KRR estimator as discussed in Appendix I.1. The optimization proceeds sequentially. At the end of each day, the measurements are added to the dataset, and the model is updated. Given the forecasted prices for the next day, the algorithm selects parameters θ that minimize Jτ (θ).

Measurements for baseline methods. All methods optimize the same time-varying objective 

Jτ (θ), but differ in the measurements available during learning. vvBO models the mapping from θ

to the full trajectories f (θ) = ( Q(t), y (t)) , and evaluates the objective through linear and nonlinear functionals of these trajectories. MTBO assumes access only to finite-dimensional task measurements, which correspond to energy consumption, environmental impact, and thermal discomfort. The surrogate objective is the weighted combination of these three measurements. BO directly observes the scalar objective value Jτ (θ). CTBO and FFBO treat the price signals as contextual variables and learn scalar surrogate models for the objective. CTBO allows the context to vary across iterations, 29 Table 5: Measurements available to each method. Method Input Measurements vvBO θ Q(t), y (t)

MTBO θ energy cost, CO 2 cost, and thermal discomfort BO θ Jτ (θ)

CTBO (θ, p heating ,τ (t), p CO 2 (t)) Jτ (θ)

FFBO (θ, p heating ,0(t), p CO 2 (t)) Jτ (θ)0 50 100 150 200           

> Iteration
> 250
> 500
> 750
> Daily cost [EUR]  high price low price
> optimal
> Active learning
> 010 20 30 40 50
> Iteration
> 200
> 400
> 600
> 800
> Daily cost [EUR]
> Validation
> vvBO (ours) CTBO MTBO [11] BO FFBO [20]

Figure 9: Daily cost during learning and validation phases. while FFBO assumes a fixed context during learning. Table 5 summarizes the measurements available to each method. 

Additional results. Fig. 9 shows the daily cost during both the learning and validation phases. In the first plot, we report the daily cost during the training phase. The black dashed line indicates the optimal cost for each day. After an initial exploration period, vvBO adjusts effectively to the time-varying objective (around iteration 150). In particular, when the heating price is high, vvBO selects parameters that reduce energy consumption by lowering the indoor temperature. Conversely, when the price is low, it increases the temperature to improve thermal comfort, thereby reducing the overall cost. In contrast, most baseline methods fail to track this optimal policy and exhibit significantly higher costs. The second plot shows the validation performance of the learned models. In this phase, no additional data are collected. Each method recommends parameters based on the trained model. vvBO consistently achieves the lowest cost across all days, demonstrating its ability to generalize and adjust to time-varying conditions. This highlights the advantage of leveraging structured measurements for efficient learning in real-world control problems. 30